# JJtomato 最新产品完整方案

> 版本：v0.1.17 基础上的下一阶段方案
>
> 方案日期：2026-08-27
>
> 核心方向：轻量 Todo + 真实专注记录 + 亲密好友陪伴

## 1. 方案结论

JJtomato 不应该继续扩展成一个复杂的项目管理软件，也不应该只是一个漂亮的番茄钟。它应该成为一个让用户每天完成下一步、记录真实进展，并能和一两个亲近的朋友安静陪伴学习的轻量桌面工具。

核心定位：

> **陪你把今天的下一步做完，也让朋友像坐在旁边一样陪你。**

产品闭环：

```text
快速记录任务
    ↓
拆成今天能做的下一步
    ↓
绑定一颗番茄并开始
    ↓
允许暂停、处理中断、继续恢复
    ↓
保存真实专注记录
    ↓
完成任务并复盘
    ↓
和朋友共享状态、互动、留言
    ↓
明天从未完成的下一步继续
```

## 2. 产品原则

### 2.1 今天优先

打开软件后首先看到“今天要做什么”和“现在做哪一步”，而不是项目、标签、报表和设置。

### 2.2 任务与专注绑定

每次专注都尽量关联一个任务或子任务，结束后留下真实的开始时间、结束时间和有效专注秒数。

### 2.3 历史不可被顺延覆盖

未完成任务可以进入明天，但已完成任务永远留在原日期。历史日期记录的是“那天发生了什么”，不是当前任务列表的镜像。

### 2.4 陪伴而非监控

好友看到的是用户主动共享的状态、当前活动、共享任务和专注结果。全局键盘监听只用于生成“正在输入”状态，不记录文字、不保存按键、不截图、不读取其他应用内容。

### 2.5 低干扰、轻量化

小猫、番茄和消息都服务于陪伴，不做信息流、排行榜、视频会议和常驻 AI 聊天。

### 2.6 温和但不幼稚

动画可以可爱，但核心反馈必须清楚、快速、可关闭。不要用“失败”“断签清零”“你又偷懒了”等制造压力。

## 3. 目标用户

第一阶段只服务以下用户：

1. 在 Windows 老电脑上学习、备考、写代码或处理日常事务的人；
2. 需要把大任务拆小，但不想维护复杂项目系统的人；
3. 想和一两个朋友一起学习，又不想开视频会议或加入陌生人社区的人。

## 4. 信息架构

顶层只保留三个入口：

```text
今天       添加任务、开始专注、查看陪伴状态
计划       查看今天、本周和本月安排
复盘       查看完成情况、专注记录和明日第一步
```

好友陪伴不单独做成复杂的社交首页，而是作为“今天”页面右下角的小猫和侧边陪伴面板存在。

## 5. 首页设计

### 5.1 今日页布局

```text
┌──────────────────────────────────────┐
│ JJtomato              今天  计划  复盘 │
├───────────────────────┬──────────────┤
│ 当前专注              │ 今日任务      │
│                       │              │
│      25:00            │ □ 完成报告    │
│  当前：整理资料        │   ├─ □ 找资料  │
│                       │   └─ □ 写摘要  │
│ [开始 / 暂停]          │ □ 整理会议记录 │
│                       │              │
│                       │        🐱     │
├───────────────────────┴──────────────┤
│ 今日专注 1小时20分 · 已食用 4 · 已消化 3 │
└──────────────────────────────────────┘
```

### 5.2 创建任务

第一步只出现一个输入框：

```text
接下来要做什么？
```

创建后再按需展开日期、优先级、番茄数量、子任务和备注。任务名称过长时默认两行省略，显示“点击展开完整名称”；展开后在卡片内部多行展示，不覆盖下方内容。

### 5.3 当前任务

开始专注前要求用户选择当前任务。计时器区域显示：

```text
当前目标：完成方案背景部分
已专注：18:42
```

用户始终知道这一颗番茄服务于什么，而不是只看到倒计时数字。

## 6. 任务与子任务规则

### 6.1 数据关系

```text
主任务：复习通信协议       2 / 4 个子任务
├─ ✓ 复习 IIC              1 / 1 个番茄
├─ ✓ 复习 SPI              1 / 1 个番茄
├─ ○ 复习 MODBUS            0 / 1 个番茄
└─ ○ 整理对比笔记           0 / 1 个番茄
```

主任务计划番茄数始终由子任务汇总：

```text
主任务计划番茄 = 子任务计划番茄之和
主任务已消化番茄 = 子任务已消化番茄之和
```

### 6.2 完成规则

- 完成一个子任务，只消化该子任务对应的番茄；主任务显示“进行中”；
- 所有子任务完成后，主任务自动完成；
- 用户可以直接完成主任务，系统需要二次确认，并将剩余子任务和番茄一并完成；
- 取消完成时，返还对应的已消化番茄，并保留专注记录；
- 删除主任务时明确提示其子任务也会被处理；
- 子任务完成后不从历史中消失，必须保留完成日期和所属主任务。

### 6.3 顺延规则

```text
8 月 25 日
主任务：复习通信协议              顺延到 8 月 26 日
├─ ✓ IIC                         留在 8 月 25 日
├─ ✓ SPI                         留在 8 月 25 日
└─ ○ MODBUS                      顺延到 8 月 26 日
```

新日期只显示未完成部分；旧日期显示当天完成的任务和子任务。严禁因为顺延导致已完成任务在两个日期都显示为完成。

## 7. 专注、暂停与复盘

### 7.1 专注

开始专注时记录：

- 关联任务和子任务；
- 计划时长；
- 开始时间；
- 当前番茄模式。

结束、暂停或提前停止时记录：

- 结束时间；
- 实际有效专注秒数；
- 暂停次数；
- 可选中断原因。

### 7.2 暂停

暂停不是失败，按钮文案使用：

```text
继续专注
记录中断原因
结束本次
```

中断原因：身体需求、工作插入、查资料、注意力走神、其他。

### 7.3 每日复盘

复盘控制在 10 秒内：

```text
今日专注：1小时40分
完成任务：3项
被打断：2次

今天真正完成了什么？
明天最重要的一件事是什么？
```

## 8. 亲密陪伴功能

功能名称建议：**桌边陪伴** 或 **陪伴房**。

产品情绪目标：

> 她没有一直看着你，但她就在旁边学习。

### 8.1 小猫状态

主页右下角放置一只 42～56px 的小猫。小猫是陪伴入口，不是装饰：

| 状态 | 小猫表现 | 文案 |
|---|---|---|
| 离线 | 趴在窗边睡觉 | 暂时不在 |
| 在线 | 抬头、耳朵动 | 已上线 |
| 准备中 | 摆好书本 | 准备开始 |
| 专注中 | 看书、戴耳机 | 正在专注 |
| 正在输入 | 双爪连续敲键盘 | 正在输入…… |
| 暂停 | 喝水、伸懒腰 | 暂时休息 |
| 完成 | 接住番茄、击掌 | 完成啦 |

默认只展示昵称和状态。好友任务名称只有在对方主动打开“共享当前任务”后才展示。

### 8.2 陪伴面板

点击小猫打开 300～360px 的侧边面板：

```text
桌边陪伴

🐱 小林
正在输入……
专注 18 分钟

[和她一起专注]
[送番茄] [递咖啡] [挥挥手]

消息
小林：我开始写作业啦
你：收到，我也开始

[输入一句话……] [发送]
```

面板不遮挡计时器和任务内容，移动端或小窗口中改为底部抽屉。

## 9. 互动功能

### 9.1 快速互动

首批提供五种动作：

- 送番茄：对方小猫接住一颗番茄；
- 递咖啡：对方小猫收到一杯咖啡；
- 挥挥手：小猫挥手回应；
- 轻敲猫爪：对方出现一次轻微的陪伴提示；
- 击掌：双方完成同一场专注后触发。

互动只发送一个轻量事件，例如：

```text
reaction = tomato
sender = userA
receiver = userB
createdAt = 时间戳
```

不发送动画文件，也不持续保持高频通信。

### 9.2 消息

消息分为两层：

1. 快捷消息：我来了、一起开始、喝口水、我还在、休息一下、加油；
2. 自定义短消息：建议单条最多 300 字，支持换行，不支持图片、文件和语音。

消息列表只保留陪伴房间上下文，不做公众号式信息流。默认不因为好友每次上线、暂停或输入而弹出系统通知，只在面板显示未读红点。

### 9.3 一起坐下

流程：

```text
你点击“和她一起专注”
    ↓
双方确认目标
    ↓
两只小猫同时坐到书桌前
    ↓
双方同时开始计时
    ↓
结束后分别回报完成情况
    ↓
小猫击掌并生成共同结果卡
```

结果卡：

```text
今天一起坐了 52 分钟
你完成 2 个任务
小林完成 1 个任务
```

## 10. 全局键盘监听方案

### 10.1 允许的目标

全局监听只用于判断：

```text
正在输入 / 暂停输入 / 空闲
```

它不用于显示或上传用户具体按下的键。

### 10.2 状态算法

```text
第一次按键
→ typing = true
→ 发送一次“正在输入”

连续 2～3 秒没有按键
→ typing = false
→ 发送一次“停止输入”
```

要求：

- 只在用户开启“亲密模式”且陪伴房间打开时启动；
- 监听器放在 Electron 主进程或独立轻量原生模块；
- 原始按键事件只在本机瞬时处理，不写入磁盘、不进日志；
- 网络只发送状态变化和心跳；
- 用户可设置暂停共享快捷键；
- 好友只看到“正在输入”，不看到输入内容和当前软件名称。

### 10.3 隐私文案

设置页明确显示：

```text
全局输入状态
好友只能看到你是否正在输入。
JJtomato 不记录按键、文字、截图或其他软件内容。

[开启亲密模式]
```

如果用户不愿意授权，则只使用 JJtomato 内部输入状态。

## 11. 隐私与安全边界

默认关闭或禁止：

- 摄像头和麦克风；
- 屏幕截图和屏幕共享；
- 具体键盘内容记录；
- 鼠标轨迹记录；
- 当前打开软件名称；
- 公开排行榜；
- 陌生人匹配。

好友权限按人设置：

```text
看见我在线                 开 / 关
看见我正在输入             开 / 关
看见我的当前活动           开 / 关
看见我的当前任务           开 / 关
接收互动动画               开 / 关
接收消息提示               开 / 关
```

好友关系采用邀请链接或邀请码，不能通过搜索手机号直接发现用户。

## 12. 技术架构

### 12.1 桌面端

v0.x 继续使用 Electron + React + Vite。当前依赖已经较少，暂不因为内存担忧立即迁移 Tauri。

Electron 主进程负责：

- 窗口生命周期；
- 计时器 deadline 和完成通知；
- 全局输入监听；
- 好友状态心跳；
- IPC 权限边界；
- 本地数据文件和备份。

React 渲染进程负责：

- 任务、计划和复盘界面；
- 小猫动画；
- 陪伴面板；
- 消息列表；
- 状态变化后的视觉反馈。

### 12.2 轻量化措施

- 主窗口和置顶小窗不要长期保持两个活跃渲染进程；
- 小窗关闭或长时间隐藏后销毁，需要时再创建；
- 将计时器从 250ms 高频刷新改为基于 deadline 的低频显示；
- 隐藏窗口允许后台节流，计时由主进程保证准确；
- 小猫动画只在状态变化和可见时运行；
- 好友状态使用 20～30 秒心跳；
- 不上传每次键盘事件，只发送 typing 状态变化；
- 历史数据按日期范围加载，不一次性渲染全部年份；
- 不在启动阶段加载 AI SDK、视频、图表和大型聊天组件。

### 12.3 服务端最小组成

当前本地存储只能支持单机使用。要实现真实好友陪伴，需要增加：

```text
账号身份
好友关系
陪伴房间
实时 presence
互动事件
短消息存储
```

建议将数据分为：

- 本地数据：任务、历史、专注记录和个人复盘；
- 房间数据：成员、状态、共同专注结果；
- 消息数据：好友主动发送的文本和互动事件。

默认不要同步用户全部任务。共享任务必须由用户主动选择。

## 13. 数据模型

```text
Task
  长期任务定义、名称、父子关系

TaskPlan
  某个日期的计划实例、预计番茄、完成状态

FocusSession
  开始时间、结束时间、实际秒数、暂停原因、关联任务

DailyReview
  当天完成内容、中断原因、明日第一步

CompanionRoom
  房间、成员、权限、创建时间、结束时间

Presence
  online、focusing、typing、paused、away、lastSeen

RoomEvent
  加入、开始、暂停、完成、送番茄、击掌等事件

Message
  房间、发送者、文本、创建时间、已读状态
```

核心原则是：任务定义、每日实例、专注记录和好友事件必须分开，避免修改今天的任务时重写历史日期。

## 14. 版本路线图

### v0.1.18：陪伴基础设施

- 陪伴面板 UI；
- 好友邀请码和房间模型；
- 在线、离线、专注、暂停状态；
- 小猫基础状态动画；
- 隐私开关；
- 本地模拟好友状态，便于先验证交互。

### v0.1.19：互动与消息

- 送番茄、递咖啡、挥手、轻敲猫爪；
- 快捷消息；
- 300 字短消息；
- 未读红点和消息已读；
- 互动冷却和防刷机制。

### v0.2.0：真实实时陪伴

- 真实账号和好友关系；
- 房间实时状态；
- “一起坐下”共同专注；
- 房间结束结果卡；
- 消息和互动的断线重连。

### v0.2.1：全局输入状态

- Windows 全局输入监听；
- 只上传 typing / idle；
- 本地原始事件不落盘；
- 一键暂停共享；
- 开机默认不启动监听；
- 杀毒软件、输入法、权限和卸载清理测试。

### v0.3：本地优先同步

- 用户主动开启云同步；
- 跨设备恢复任务和历史；
- 冲突保留而非静默覆盖；
- 完整数据导出和删除；
- 低网速和断网恢复。

### v0.4：显式 AI 工具

- 将大任务拆成下一步；
- 根据剩余时间挑选今日任务；
- 自动整理每日复盘；
- 生成起步动作；
- 默认不常驻、不内置模型、不自动上传全部任务。

## 15. 性能验收目标

以下是开发验收目标，必须在实际 Windows 老电脑上测量，不把理论值当作结果：

| 指标 | 目标 |
|---|---|
| 冷启动到可操作 | ≤ 2.5 秒 |
| 主窗口空闲内存 | 目标 ≤ 150 MB RSS，需实机验证 |
| 空闲 CPU | 平均 ≤ 1% |
| 全局输入监听额外 CPU | 平均 ≤ 1% |
| 小猫动画运行 CPU | 平均 ≤ 2% |
| 连续运行 8 小时内存增长 | ≤ 10 MB |
| 断网核心能力 | 计时、任务、历史、导出仍可用 |
| 输入状态延迟 | 实时通道下目标 ≤ 1 秒 |
| 停止输入判定 | 约 2～3 秒无输入后恢复 idle |

如果 Electron 在目标设备上仍无法达到预算，再单独评估 Tauri；迁移必须保持旧数据格式和 Windows 功能兼容。

## 16. 功能验收标准

### 单机核心

- 已完成任务不会顺延到第二天；
- 25 日和 26 日不会出现同一完成记录的错误复制；
- 历史日期能看到当天真实任务和专注记录；
- 空格可以暂停和恢复，但输入框内不会误触发；
- 长任务名称可以省略、提示和展开，展开不覆盖下方内容；
- 日历视图日期、任务、统计文字不重叠。

### 陪伴功能

- 朋友上线、离线、专注、暂停和输入状态能正确切换；
- 送番茄后对方小猫有明确但短暂的反应；
- 消息发送、已读和断线重连可用；
- 全局监听关闭后不再产生输入状态；
- 网络中不存在原始键值和输入文字；
- 用户可以分别关闭任务共享、输入共享、互动和消息提示。

## 17. 明确暂缓的功能

v1.0 之前暂不做：

- 视频、语音和摄像头陪伴；
- 屏幕共享；
- 公开社交广场；
- 陌生人匹配；
- 复杂群聊、文件和图片传输；
- 公开排行榜和惩罚性连续签到；
- 本地大模型和常驻 AI；
- 复杂项目依赖图和多人项目权限；
- 全局应用监控和具体键盘记录。

## 18. 成功指标

最重要的不是打开次数，而是用户是否真的完成了事情：

- 第一颗番茄完成率；
- 第一个任务完成率；
- 暂停后恢复率；
- 每周有效专注时长；
- 任务完成后继续开始下一任务的比例；
- 日复盘完成率；
- 邀请朋友后共同专注完成率；
- 房间结束回报率；
- 低端电脑启动时间、RSS、CPU 和 8 小时内存曲线。

北极星指标：

> **每周与具体任务绑定的有效专注时长，以及因此完成的真实任务数。**

## 19. 最终产品形态

```text
JJtomato
├─ 今天：任务 + 当前专注 + 陪伴小猫
├─ 计划：日 / 周 / 月计划，不伪装成复杂甘特图
├─ 复盘：真实完成、实际专注、中断和明日第一步
├─ 桌边陪伴：在线、专注、正在输入、小猫动画
├─ 轻互动：送番茄、咖啡、挥手、击掌
└─ 短消息：在陪伴房内进行简短交流
```

最终的核心体验是：

> 打开 JJtomato，知道今天先做什么；开始专注时，知道朋友也在；暂停时不会被惩罚；完成后能看见真实进展；朋友可以送来一颗番茄，而不是打断你的一整段专注。
