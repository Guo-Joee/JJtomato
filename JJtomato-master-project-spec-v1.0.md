# JJtomato 主项目规格（唯一跨模块规则）

> 文档版本：v1.0<br>
> 日期：2026-08-27<br>
> 当前应用版本：v0.1.20<br>
> 目标读者：产品设计、桌面端开发、服务端开发、测试与未来维护者<br>
> 状态：**开发基线**。本文定义跨模块行为；尚未实现的功能均明确标为“计划”。

## 1. 文档优先级与不冲突原则

从现在开始按下表解释项目文档，避免同一功能有两种口径：

| 优先级 | 文档 | 作用 |
| --- | --- | --- |
| 1 | 本文 | 产品边界、数据归属、状态机、权限、版本顺序和跨模块验收的唯一规则 |
| 2 | `JJtomato-study-room-avatar-spec-v0.1.md` | 本文第 11 节“自习室与形象”的资源格式、AI 创建、导入导出、上传同步细节 |
| 3 | 源代码与自动化测试 | 当前已实现行为的事实来源；若与本文冲突，应记录差异并按 Wave 迁移，不能静默假装已实现 |
| 4 | `JJtomato-latest-product-solution.md` | 产品方向和早期交互说明；和本文不同处以本文为准 |
| 5 | `JJtomato-market-research-and-roadmap.md` | 市场调研、取舍依据和备选路线，不直接定义运行行为 |

本项目遵守四条总规则：

1. **本地任务优先，好友陪伴可选。** 断网、退出账号、好友不在线时，任务、专注、历史、导出仍可正常使用。
2. **一个事实只由一个模型拥有。** 任务定义、每日计划、专注记录、房间状态、形象资源彼此引用，不互相复制。
3. **实时状态不是历史。** `Presence` 可过期和覆盖；`FocusSession`、`TaskPlan`、消息、反应事件一经确认不可被在线状态回写。
4. **陪伴不是监控。** 用户明确授权才共享状态；不收集按键内容、屏幕、摄像头、麦克风、鼠标轨迹或当前软件名称。

---

## 2. 项目启动信息

| 项目项 | 规格 |
| --- | --- |
| 产品名称 | JJtomato |
| 项目类型 | Windows 本地优先的 Electron 桌面应用；后续增加可选实时陪伴服务 |
| 核心用户 | 使用旧电脑学习、写作、写代码或安排日常任务，希望和熟人安静互相陪伴的人 |
| 业务目标 | 帮用户明确今天下一步、记录真实专注和历史进展；让 1–4 位亲近朋友低干扰地彼此陪伴 |
| 终极体验 | 打开即知道先做什么；开始时知道朋友也在；暂停不会被惩罚；完成后保留真实记录 |
| 技术基线 | Electron 43、React 18、Vite 8、纯本地数据；无现成后端 |
| 性能约束 | 面向老电脑，核心流程不依赖常驻 AI、视频、音频、完整聊天系统或高频动画 |
| 当前已实现 | 番茄计时、任务/计划/复盘、历史顺延修复、置顶小窗、本地好友演示、消息与互动演示 |
| 当前未实现 | 账号、真实好友、真实房间、网络同步、资源上传、全局输入监听、AI 形象包解析 |

### 2.1 范围

**本项目 v1.0 之前必须完成：**

- 本地 Todo、子任务、日期计划、真实专注记录、每日复盘、可导出；
- 明确的暂停、恢复、结束和历史顺延规则；
- 熟人好友、1–4 人实时自习室、短消息和轻互动；
- 可关闭的桌边陪伴与主页面自习室；
- 默认形象与自定义轻量形象包；
- 账号和房间数据的最小同步、断线重连、权限与隐私控制。

**明确不做：**

- 视频、语音、屏幕共享、公开直播、陌生人匹配、公开广场、排行榜；
- 大型项目管理、复杂甘特图、团队审批、多人文件协作；
- 图片/文件/语音聊天、群公告、社交动态；
- 常驻本地模型、自动上传全部任务给 AI、自动监听所有键盘内容；
- 让一个人同时被多个房间的番茄钟控制。

---

## 3. 产品定位与成功标准

### 3.1 一句话定位

> **JJtomato 是一款轻量的日常任务与专注工具：你做自己的计划，朋友安静地坐在旁边。**

它不是 Todoist 的项目管理替代品，也不是 Discord/微信的聊天替代品，更不是线上自习直播间。所有新功能必须能回答：它是否让用户“更容易开始下一步、坚持一轮、看到真实进展或感到被陪伴”？不能回答的功能不进入 v1.0。

### 3.2 北极星指标与保护指标

| 类型 | 指标 | 判断方式 |
| --- | --- | --- |
| 北极星 | 每周与具体任务关联的有效专注分钟数 | 已完成/结束的 `FocusSession.activeSeconds` 汇总 |
| 核心 | 首颗番茄完成率 | 启动后实际有效专注 ≥ 15 分钟的比例 |
| 核心 | 暂停后恢复率 | 暂停后 24 小时内恢复或新开专注的比例 |
| 核心 | 历史可信度 | 顺延后已完成条目重复出现的缺陷数必须为 0 |
| 陪伴 | 共同专注完成率 | 明确加入同一共同会话且至少一人完成的比例 |
| 保护 | 空闲 CPU / 内存 | 见第 16 节性能预算 |
| 保护 | 隐私事故 | 原始按键、未授权任务标题、资源 URL 泄露必须为 0 |

指标仅用于改进体验，不展示公开排名，不用来惩罚用户或好友。

---

## 4. 信息架构与界面规则

### 4.1 顶层入口

```text
今天 ── 当前任务、专注、今日任务、当前自习室入口
计划 ── 日 / 周 / 月的计划浏览与调整
复盘 ── 当日真实完成、专注记录、明日第一步
设置 ── 计时、外观、数据、隐私、陪伴、形象
```

好友与聊天不是第四个复杂社交首页。它们从“今天”的右侧陪伴区进入；设置与主流程分开，避免用户想开始专注时先面对大量配置。

### 4.2 首页优先级

首页按下列顺序排布，空间不足时从下往上折叠：

1. 当前专注和开始/暂停/结束控制；
2. 今天任务（含下一步）；
3. 今日真实统计；
4. 陪伴入口与自习室摘要；
5. 装饰性背景和动画。

任何窗口尺寸下都不得让装饰、好友卡片、状态文字或日历覆盖计时控制与任务内容。好友区域依据人数自适应：0 人隐藏、1 人一列、2–4 人网格自然高度、超过 4 人后才滚动；不得预留一大片空白或把单个好友挤成窄卡。

### 4.3 计划视图而非甘特图

JJtomato 的计划页采用“日 / 周 / 月 + 时间块提示”，不是传统工程甘特图：

- **日视图**：按时间段或优先级显示今天的任务和专注记录；
- **周视图**：每一天显示未完成计划数量、已完成数和总专注；
- **月视图**：显示每日完成和专注热度，点击进入日详情；
- **长任务**：显示开始日期、计划日期、顺延次数和进度，不绘制横跨数周的粗条；
- **可用空间不足**：任务名两行省略，使用渐隐/省略号和“点击展开”，展开只能在原卡片内向下推动内容，不能遮挡下方卡片。

原因：用户在安排日常，而不是维护依赖关系图。该决定可在未来有明确项目协作需求时重新评估，但不能把当前日历重新做成难读的甘特图。

### 4.4 文案和交互基线

| 场景 | 正确文案/行为 | 禁止 |
| --- | --- | --- |
| 暂停 | “继续专注”“记录中断原因”“结束本次” | “失败”“放弃” |
| 未完成顺延 | “移到明天继续” | 把已完成任务复制到明天 |
| 好友离线 | “暂时不在” | 用红色警告或强弹窗 |
| 状态不可用 | “仅本机可见”“等待同步” | 编造朋友的实时状态 |
| 长任务名 | 省略 + 展开提示 + 卡片内多行 | 截断后无法查看全文 |
| 同伴互动 | 短暂动画 + 面板记录 | 全屏打断、声音强提醒 |

---

## 5. 核心领域模型：任务、计划、专注和复盘

### 5.1 实体归属

```text
Task（长期定义） 1 ── N TaskPlan（某天的计划实例）
Task 1 ── N Subtask（子任务定义）
TaskPlan / Subtask 0 ── N FocusSession（真实过程记录）
Date 1 ── 1 DailyReview（当天复盘）
```

| 实体 | 拥有的数据 | 绝不保存的内容 |
| --- | --- | --- |
| `Task` | 名称、备注、父子关系、创建时间、是否归档 | 某一天是否完成、顺延历史的副本 |
| `TaskPlan` | `planDate`、计划番茄、当天状态、来源计划 ID、显示顺延信息 | 对原日期历史的覆盖数据 |
| `FocusSession` | 关联目标、开始/结束、有效秒数、暂停段、结束原因 | 好友当前在线状态 |
| `DailyReview` | 当天总结、明日第一步、统计快照 | 可修改的其他日期计划 |

`TaskPlan` 是“某日准备做什么”的实例，`FocusSession` 是“实际上发生了什么”的不可变记录。它们不能互相替代。

### 5.2 任务与子任务

```ts
type Task = {
  id: string;
  title: string;                 // 1–120 字符
  note: string | null;
  createdAt: string;
  archivedAt: string | null;
  parentTaskId: string | null;
  sortKey: number;
};

type TaskPlan = {
  id: string;
  taskId: string;
  planDate: 'YYYY-MM-DD';
  plannedPomodoros: number;      // 0–99
  status: 'open' | 'completed' | 'cancelled';
  completedAt: string | null;
  carriedFromPlanId: string | null;
  carriedCount: number;
};
```

- 子任务是 `parentTaskId` 指向主任务的 `Task`，各自拥有独立 `TaskPlan` 和完成时间。
- 若主任务有子任务，主任务计划番茄和已完成进度由可见子任务汇总；直接完成主任务时必须二次确认并明确将如何处理未完成子任务。
- 取消完成只改变当前计划状态或子任务状态；过去的专注记录保留，不“返还”已发生的分钟数。
- 删除改为归档；归档前提示关联计划和历史会保留。物理删除仅在“数据管理”中做并必须导出/确认。

### 5.3 顺延：唯一正确算法

顺延只针对某一天未完成的 `TaskPlan`，从不复制已完成计划，也从不修改过去日期：

```text
8 月 25 日 TaskPlan A：open  ───> 新建 8 月 26 日 TaskPlan B
                                B.carriedFromPlanId = A.id
8 月 25 日 TaskPlan C：completed ─> 保留 C，不创建 8 月 26 日副本
```

规则：

1. 同一 `taskId` 在同一 `planDate` 最多一个未取消计划；创建前必须去重。
2. 顺延时只复制“未完成工作量”，新计划的 `carriedCount = 原计划.carriedCount + 1`。
3. 已完成计划永远留在原日期；同一已完成 `TaskPlan` 不得在两个日期显示为完成。
4. 如果目标日期已有该任务的 `open` 计划，则合并未完成工作量并记录来源，不新增重复卡片。
5. 取消的计划不自动顺延；用户可以手动重新安排。
6. 历史页按 `planDate` 查询真实实例，不用当前任务列表回放历史。

### 5.4 任务行为契约

| 场景 | Given | When | Then |
| --- | --- | --- | --- |
| 当天完成 | 25 日计划未完成 | 用户勾选完成 | 只更新 25 日计划，记录完成时间 |
| 隔日顺延 | 25 日计划未完成 | 用户或日结执行顺延 | 生成/合并 26 日未完成计划，25 日仍保留未完成历史 |
| 已完成不顺延 | 25 日计划已完成 | 执行顺延 | 26 日不产生该完成计划 |
| 再次打开历史 | 已有 25/26 日记录 | 浏览 25 日 | 只看见 25 日当天计划和会话 |
| 长名称 | 标题超过两行 | 初始渲染 | 显示省略提示；点击后在卡片内完整展开 |

---

## 6. 专注计时、暂停和快捷键

### 6.1 计时器唯一权威

Electron 主进程保存当前阶段的 `deadline`，是唯一计时权威。任何 React 窗口、置顶小窗、隐藏/最小化页面只能渲染剩余时间，不能各自累积秒数或反向发布默认值。

```ts
type TimerState = {
  phase: 'work' | 'short_break' | 'long_break';
  status: 'idle' | 'running' | 'paused';
  durationSeconds: number;
  remainingSeconds: number;
  deadlineAt: number | null;
  activeFocusSessionId: string | null;
};
```

### 6.2 状态机

```text
idle --开始--> running --暂停--> paused --继续--> running
  ^              |                  |                |
  └--重置/结束---┴------------------┴---自然结束-----┘

自然结束：work -> short_break / long_break；休息结束 -> work
```

- 开始工作阶段前 SHOULD 选择一个任务或允许“无任务快速专注”；无任务会话在复盘里明确标注“未关联任务”。
- 暂停记录 `PauseInterval`，有效专注秒数只累计运行区间。
- 重置不删除既有历史会话；如果当前运行不足最小记录阈值（默认 60 秒），可标记为 `discarded` 并不计入统计。
- 自然结束与手动结束都必须关闭当前会话、写最终有效时长、清理共同专注关联，再切换下一阶段。

### 6.3 键盘与输入框冲突

- `Space`：当焦点不在输入框、文本编辑器、可编辑元素或下拉选择组件内时，开始/暂停/继续。
- `R`：同样仅在非编辑控件焦点时重置，且重置运行中的会话必须确认。
- 快捷键注册要使用事件的 `code`/组合键规则并在捕获阶段检查目标元素；不能因为好友聊天输入框被打开而暂停计时。
- 未来全局键盘监听与应用内快捷键是两个模块：前者只汇总输入状态，后者控制软件；不可复用同一原始按键日志。

### 6.4 专注行为契约

| 场景 | Then |
| --- | --- |
| 主窗口隐藏 30 分钟后恢复 | 剩余时间由 deadline 计算，未出现漂移或停止 |
| 空格在消息框输入时 | 插入空格，不控制计时 |
| 暂停共同专注 | 自己显示暂停；朋友继续自己的计时和状态 |
| 重置/跳过/切换模式/自然结束 | 当前 `focusSessionId` 和共同专注关系统一收尾 |
| 小窗打开 | 只读取主进程最新状态，不覆盖主窗口状态 |

---

## 7. 复盘、统计、导出和数据生命周期

### 7.1 每日复盘

复盘必须在 10 秒内可完成，内容为：今日有效专注、完成任务数、暂停/中断次数、自由文本“今天真正完成了什么”、明日最重要的一步。系统 MAY 提供自动汇总，但绝不自动编造用户感受。

### 7.2 统计口径

- 有效专注：已结束 `FocusSession.activeSeconds`；暂停时间不计入。
- 完成任务：当日 `TaskPlan.completedAt` 落在当天的计划数，不能通过当前任务状态反推。
- 顺延数：新建/合并计划时记录的 `carriedCount`，不等于失败次数。
- 共同专注结果：每人只展示自己的完成时长和用户授权的结果摘要；不比较排名。

### 7.3 导出、备份、删除

| 功能 | 规则 |
| --- | --- |
| Markdown 导出 | 用户可选日期导出计划、完成和专注；不默认导出好友消息或头像资源 |
| 本地备份 | 使用版本化 JSON 快照；导入前校验 schema 并先创建恢复点 |
| 数据导出 | 分为“个人效率数据”“陪伴消息”“我的形象包”，用户逐项选择 |
| 删除账户 | 先导出提示；本地任务保留/删除由用户选择；服务端按第 15 节的保留规则处理 |
| 清空缓存 | 只清头像和临时资源；不能清任务、历史或会话 |

---

## 8. 好友、权限、消息与轻互动

### 8.1 好友关系

好友必须通过邀请码/邀请链接建立，并需要双方接受。不得通过手机号搜索、公开发现或自动推荐陌生人。好友关系和房间成员资格不同：成为好友不等于自动进入每个房间。

```text
邀请 -> 受邀方确认 -> FriendRelation(active)
创建自习室 -> 发出 RoomInvite -> 受邀方确认 -> RoomMember(active)
```

### 8.2 权限按人、按房间、默认关闭

每位好友的权限至少包括：

| 字段 | 默认 | 说明 |
| --- | --- | --- |
| `shareOnline` | 开 | 仅“在线/暂时不在” |
| `shareActivity` | 开 | 准备、专注、休息；不含任务名 |
| `shareTyping` | 关 | 只分享 typing/idle，不含按键内容 |
| `shareTaskTitle` | 关 | 当前任务标题，必须额外开启 |
| `allowReactions` | 开 | 番茄/咖啡/挥手等轻互动 |
| `allowMessages` | 开 | 最多 300 字的文字消息 |
| `shareAvatar` | 关 | 允许对方按需下载自定义形象 |

房间权限只能收窄好友权限，不能放大。例：甲对乙关闭 `shareTyping`，任何含乙的房间都不能让乙看到甲的输入状态。

### 8.3 消息与反应

- 普通消息：UTF-8 文本，1–300 字，支持换行；不支持附件、富文本、链接预览或图片。
- 快捷消息：我来了、一起开始、喝口水、我还在、休息一下、加油。
- 反应：`tomato`、`coffee`、`wave`、`paw`、`highfive`；是可去重的短事件，不是持续动画。
- 消息以 `messageId` 和客户端幂等键去重；重新连接不重复显示或重复送礼。
- 默认只有面板未读标记，不因每次输入、上线、休息发系统通知。用户可在设置中为“直接消息”和“共同专注邀请”单独开启通知。

### 8.4 共同专注

共同专注不是双方共用同一计时器，而是多个独立计时器加入同一个 `sharedFocusId`：

```text
甲发起邀请 -> 乙确认 -> 为成员创建同一 sharedFocusId
各端分别开始自身 Timer / FocusSession
任一人暂停、结束、掉线 -> 只影响自己的会话与 Presence
所有人结束或房主结束房间 -> 生成只读结果摘要
```

因此“朋友暂停”不能把用户的番茄钟暂停；“朋友离开”不能把用户的任务置为未完成。若用户只想安静同坐，可不发送邀请，直接在同一房间分别专注。

---

## 9. Presence、全局输入状态与多房间

### 9.1 Presence 是可过期快照

```ts
type PresenceActivity =
  | 'offline' | 'online' | 'ready' | 'focusing'
  | 'typing' | 'browsing' | 'short_rest' | 'long_rest';

type Presence = {
  memberId: string;
  roomId: string;
  activity: PresenceActivity;
  focusSessionId: string | null;
  sharedFocusMemberIds: string[];
  stateVersion: number;
  occurredAt: string;
  expiresAt: string;
};
```

状态优先级从高到低：隐身/过期 → `offline`；长休 → `long_rest`；短休 → `short_rest`；专注且近期输入 → `typing`；专注且授权浏览状态 → `browsing`；专注 → `focusing`；已入房 → `ready`；其余在线 → `online`。

每个成员的 `stateVersion` 单调递增。接收方 MUST 丢弃版本更低或同版本且时间更早的事件；心跳过期后本地降级为离线，但不删除历史会话。

### 9.2 全局键盘监听（计划功能）

仅在用户手动开启、对应好友权限允许、至少一个相关房间为当前房间时运行：

```text
本机按键事件 -> 只更新 lastInputAt（内存）
首次输入/idle 变为 typing -> 发送一个 Presence 更新
连续 2–3 秒无输入 -> 回退为底层专注/在线状态并发送一次更新
```

严格禁止：保存具体键值、文字、组合键、窗口标题、应用名、截图；禁止把原始事件发到 React renderer、日志、磁盘、服务器或好友端。关闭权限、退出当前房间、进入隐身、应用退出时立即停止监听并清空内存时间戳。

### 9.3 多房间

- 用户可加入多个房间，但仅有一个 `activeRoomId`。
- 主页面可浏览其他房间，浏览不改变自己的 Presence 和桌边小窗。
- 计时中切换当前房间必须让用户选择“结束本轮后切换”或“暂停并切换”；不允许静默自动切换。
- `activeRoomId` 驱动本人 Presence、置顶小窗和共同专注邀请；其他房间仅接收/浏览信息。

这条规则解决“同时创建两三个自习室、朋友有三四个”时状态和画面互相覆盖的问题。

---

## 10. 自习室与两种陪伴场景

### 10.1 一个状态，两种视图

```text
Profile.avatarRef + activeRoom.Presence + TimerState
                    │
                    └── AvatarRenderer ──> 主页面自习室
                                         └─> 置顶小窗桌边陪伴
```

主页面自习室是固定座位场景，最多 8 名成员；置顶小窗以倒计时为中心，最多 4 名用户明确勾选的朋友。两个场景永远渲染相同的 `avatarId + revision + activity`，但可以根据空间使用不同缩放、静态预览或固定道具。

### 10.2 场景规则

| 场景 | 用户目的 | 人数与性能 | 不能做什么 |
| --- | --- | --- | --- |
| 主页面自习室 | 看谁在场、进入聊天、获得空间感 | 2×4 座位，最多 4 个全动画，其余静态预览 | 不能靠场景修改 Presence |
| 置顶小窗 | 专注时轻量陪伴 | 0–4 位，最多 2 个全动画 | 不能显示完整聊天、任务内容、教室背景 |
| 好友列表/聊天 | 选择朋友、读消息 | 使用 `preview.webp` 静态图 | 不能加载全部 atlas |

固定电脑、键盘、咖啡、坐垫、桌面和状态气泡属于场景资产；角色包只含动物本体。这样每位朋友可用猫、狗、番茄或植物，但在两个场景中都是同一个角色。

### 10.3 自定义形象

形象包、AI 提示词、`manifest.json`、文件尺寸、导入器校验、上传和好友资源同步，均以 `JJtomato-study-room-avatar-spec-v0.1.md` 为准。主规格中只重申四个不可违反点：

1. `.jjtpet` 只含 `manifest.json`、`preview.webp`、`atlas.webp`，包内不执行代码；
2. 图像资源和 Presence 分通道同步，图片不通过 WebSocket；
3. 每次改图创建不可变 `avatarId@revision`，下载校验哈希后原子切换；
4. 自定义形象默认仅本机可见，分享给好友必须显式授权。

---

## 11. 技术架构与模块边界

### 11.1 当前和目标架构

```text
Electron Main Process
├─ 窗口、托盘、计时 deadline、全局输入监听（计划）
├─ 本地加密凭据、文件读写、备份、mini 窗口 IPC
└─ 网络连接生命周期（计划）

React Main Renderer
├─ Today / Plan / Review / Settings
├─ TaskStore、FocusStore、CompanionStore
└─ 仅通过 preload 暴露的受限 IPC 与主进程通信

Mini Renderer
├─ 倒计时与桌边陪伴视觉
└─ 只接收精简 TimerState + CompanionSnapshot

Future Companion Service（计划）
├─ 身份、好友、房间、Presence、消息、反应
├─ 形象权限与资源元数据
└─ 对象存储/CDN：仅存 preview/atlas 文件
```

### 11.2 前端模块归属

| 模块 | 唯一职责 | 不得做 |
| --- | --- | --- |
| `daily-todo` | Task、TaskPlan、顺延、历史查询 | 管理好友 Presence |
| `timer` | 计时状态、deadline、会话切换 | 决定任务是否完成 |
| `storage` | 本地 schema、迁移、备份恢复 | 将所有数据自动上传 |
| `companion` | 本地演示/未来房间状态归一化 | 读取原始键盘值 |
| `avatar`（计划） | 包校验、缓存、动作映射 | 决定谁是好友/房间成员 |
| `room`（计划） | Room、成员、权限、Presence、消息 | 持有个人全部任务 |
| `App.jsx` | 布局和用户交互编排 | 写复杂业务规则副本 |
| Electron main | 系统能力与计时权威 | 直接操作 DOM |

依赖方向必须单向：`renderer UI -> core domain -> storage/preload -> Electron main -> optional network`。底层模块不能导入 React 组件，mini 窗口不能调用主窗口内部状态。

### 11.3 本地存储与迁移

- 所有持久化数据有 `schemaVersion`；启动时执行可逆/可备份迁移。
- 每次迁移前自动创建一份版本化备份；失败回滚到备份并显示明确错误。
- 当前 localStorage 演示数据迁移到应用数据目录时，保留只读导入器；迁移完成后再逐版本废弃旧键，不直接清空。
- 任务、会话、复盘的本地 ID 使用 UUID；服务端 ID 与本地 ID 映射不能覆盖原始本地记录。

### 11.4 网络与后端最小接口（计划）

后端只接管“可选陪伴数据”，不接管个人任务库。建议 REST + WebSocket：

| 类别 | 目标接口/事件 | 关键规则 |
| --- | --- | --- |
| 身份 | `POST /v1/auth/*` | token 仅存主进程安全存储，不放 renderer/localStorage |
| 好友 | `POST /v1/friend-invites`、接受/撤销 | 双方确认，权限默认最小 |
| 房间 | `POST /v1/rooms`、加入/离开/成员列表 | 成员资格与好友关系独立 |
| Presence | WebSocket `presence.updated` | `stateVersion` 去重、心跳过期、无图片 |
| 共同专注 | `shared-focus.invited/joined/left` | 每人独立计时，不远程控制对方计时器 |
| 消息/反应 | `message.created`、`reaction.created` | `clientEventId` 幂等、长度/频率限制 |
| 形象 | 上传 init/complete、受权下载 | 见形象专门规格，资源走对象存储 |

所有写接口使用 `clientEventId` 幂等键；网络超时可安全重试同一个键，权限错误/4xx 不自动重试。

---

## 12. 同步、一致性与离线策略

### 12.1 数据同步范围

| 数据 | 存放与同步策略 |
| --- | --- |
| Task / TaskPlan / FocusSession / Review | 本地优先；云同步属于后续独立功能，默认不上云 |
| Room / Friend / Permission | 服务端权威，本地缓存只用于展示 |
| Presence | 服务端转发的可过期实时快照，客户端可在过期时降级 |
| 消息/反应 | 服务端持久化，客户端按 `messageId/clientEventId` 去重 |
| Avatar 元数据 | 服务端权威；文件按权限和可见性下载 |
| Avatar 文件 | 受保护对象存储 + 本地 LRU 缓存 |

### 12.2 冲突规则

| 冲突 | 决策 |
| --- | --- |
| 两个本地任务编辑 | 在本地优先阶段不发生跨设备合并；未来云同步保留冲突副本，禁止静默覆盖 |
| Presence 乱序 | 以 `stateVersion` 最大者为准 |
| 消息重复投递 | 以 `clientEventId` 幂等去重 |
| 好友权限变化与旧缓存 | 新权限立即阻止后续请求；已缓存图只在清理周期内本地可见，明确告知限制 |
| 形象新版下载慢 | 仍显示已验证旧版或默认形象；新文件完整校验后才切换 |
| 共同专注成员断线 | 仅该成员 Presence 过期；其他人继续，重连后不得补造“已完成”事件 |

### 12.3 断网体验

- 计时、任务、计划、复盘、导出、已缓存形象必须可离线工作。
- 断网时陪伴面板显示“未连接，状态仅本机可见”，而非伪装成朋友离线。
- 可安全重试的消息/反应放入有限队列，带幂等键和过期时间；超过时丢弃并提示“未发送”，不无限积压。
- 不在断网恢复时把离线期间所有键盘活动补发给好友；最多恢复当前状态。

---

## 13. 隐私、安全与滥用防护

### 13.1 信任边界

```text
原始输入/本地任务 ──仅本机──> Electron main / 本地数据
                             │（用户选择的摘要）
                             v
                    Renderer -> HTTPS/WSS -> 陪伴服务 -> 已授权好友
                                              │
                                              v
                                      受保护形象资源存储
```

### 13.2 必须落实的安全规则

- preload 使用 `contextIsolation`，只暴露白名单 IPC；renderer 不接触 Node、文件路径、token 和原始输入事件。
- 所有用户消息按纯文本渲染，禁止 `dangerouslySetInnerHTML`；昵称、任务名、形象名同样按文本处理。
- `.jjtpet` 只接受三文件白名单、固定尺寸/MIME/哈希/解压上限；不执行包内脚本或 SVG。
- token、签名 URL、错误详情不得写入用户可见日志；下载 URL 短时有效且不经聊天转发。
- 消息、反应、邀请、头像上传均有长度、频率和权限限制；对方拉黑或移除后立即停止请求。
- 用户可以逐项关闭在线、活动、输入、任务标题、形象、互动、消息提示；关闭必须即时生效并有可观察状态。

### 13.3 数据保留

个人任务由用户管理。服务端陪伴数据的默认策略：Presence 只保留短期运行态；消息和房间结果按用户数据设置保留；头像资源在撤销分享后停止新分发，达到保留期并无 Profile 引用后删除。具体保留时长进入上线前的隐私政策和数据库迁移，不在客户端硬编码。

---

## 14. 性能与资源预算

| 指标 | 目标 | 验证条件 |
| --- | --- | --- |
| 冷启动到可操作 | ≤ 2.5 秒 | 目标 Windows 老电脑，安装版首次外测另计 |
| 主窗口空闲 RSS | 目标 ≤ 150 MB | 不打开 mini 窗口、无全动画好友 |
| 空闲 CPU | 平均 ≤ 1% | 5 分钟稳定采样 |
| 小窗 + 2 动画角色 CPU | 平均 ≤ 2% | 倒计时运行、前台可见 |
| 8 小时内存增长 | ≤ 10 MB | 计时、隐藏/恢复、小窗开关循环 |
| Presence 状态目标延迟 | ≤ 1 秒 | 正常网络；失败时明确降级 |
| 输入状态回退 | 2–3 秒无输入 | 授权开启时的状态测试 |

实现策略：主进程 deadline、低频 UI 刷新、不可见时冻结动画、静态场景背景、最多 4 个主页面全动画和 2 个小窗全动画、头像 LRU 缓存上限 50 MB、懒加载 atlas、禁止启动即加载 AI SDK/视频/大图表。达不到预算时先进入“极简模式”（静态头像 + 状态点），而不是牺牲计时和数据正确性。

---

## 15. 版本路线与依赖顺序

版本号按当前 `0.1.19` 起算。每一阶段完成后才进入下一阶段，不能为了界面效果跨越数据模型。

| 版本/Wave | 目标 | 必须先完成 | 验收出口 |
| --- | --- | --- | --- |
| v0.1.20 | 本地核心稳定化 | 现有计时/任务测试 | 顺延、历史、空格、长标题、日周月布局回归全通过 |
| v0.1.21 | 本地房间模型替换演示状态 | `Room`/`Presence` 纯函数与迁移 | 单人多房间仅一个 `activeRoomId`；共同专注收尾统一 |
| v0.1.22 | 离线形象基础 | JJPET-Lite 校验器、默认角色 | 导入/导出/坏包回退、双场景同角色均通过 |
| v0.2.0 | 真实账号、好友、房间和 Presence | 权限、服务端最小接口、断线策略 | 两台设备状态同步、重连、去重、隐私开关通过 |
| v0.2.1 | 消息、互动、共同专注联网 | 幂等事件和通知策略 | 不重复消息/反应；一人暂停不影响另一人 |
| v0.2.2 | 形象授权上传与好友同步 | 对象存储、资源签名、缓存 | 按需下载、哈希校验、撤销分享、旧版回退通过 |
| v0.2.3 | 可选全局输入状态 | 原始输入零落盘审计、权限模型 | 只见 typing/idle；关闭即停止；无按键泄露 |
| v0.3.0 | 个人数据可选跨设备同步 | 冲突副本策略、备份恢复 | 任务历史无静默覆盖，用户可导出/恢复 |
| v0.4.0 | 显式 AI 辅助 | 明确选中数据、确认界面 | AI 不常驻、不自动上传任务，可随时关闭 |

**不允许的顺序：** 不能在没有 Room/Presence 模型时做联网小窗；不能在没有权限和资源校验时开放头像上传；不能在没有隐私审计时加入全局键盘监听；不能用真实好友网络功能替代现有本地任务回归测试。

---

## 16. 测试、发布和回滚

### 16.1 必测矩阵

| 领域 | 必测场景 |
| --- | --- |
| 任务 | 创建/编辑/归档、子任务汇总、完成取消、目标日已有计划时顺延合并、历史查询 |
| 计时 | 工作/短休/长休、暂停/恢复、快捷键编辑焦点、隐藏恢复、mini 窗口读取 |
| UI | 420×600、440×700、900×680、高 DPI、长任务名展开、好友 0/1/2–4/5+ 人 |
| 房间 | 多房间浏览/切换、Presence 乱序/过期、共同专注暂停/离开/重连 |
| 消息 | 重复投递、离线队列、长度限制、未读滚动至最新、关闭通知 |
| 形象 | 正确包、尺寸错误、白底、坏 ZIP、版本切换、撤销权限、缓存回退 |
| 隐私 | 各开关即时生效、全局输入无原始值落盘、任务标题默认不共享 |
| 性能 | 见第 14 节，含 1/4/8 人显示与极简模式 |

### 16.2 发布门禁

每次 Windows 发布前必须：

1. `npm test`、`npm run build`、安装包构建通过；
2. 用干净用户数据启动、升级已有数据启动、回滚到上一稳定安装包各验证一次；
3. 执行核心烟雾流程：新建任务 → 专注 → 暂停 → 恢复 → 完成 → 查看历史 → 打开/关闭小窗；
4. 联网版本追加两台设备的好友授权、状态、消息和断网重连检查；
5. 记录构建号、Git 提交、迁移版本、测试结果和已知限制。

### 16.3 回滚原则

- 纯 UI/逻辑版本：保留上一安装包，发布后异常立即回退；本地数据迁移必须可读旧格式。
- 本地 schema 迁移：先备份；迁移失败自动恢复，不删除用户数据。
- 服务端接口：新字段向后兼容，先灰度并允许旧客户端；接口错误率/Presence 连接异常超阈值时关闭新功能开关，核心本地功能不受影响。
- 形象服务故障：回退默认角色/缓存，不阻塞聊天、房间和计时。

---

## 17. 端到端验收场景

### 场景 A：个人的一天

```text
用户创建“整理资料” -> 计划在 8 月 25 日 -> 开始 25 分钟专注
-> 暂停去喝水 -> 恢复 -> 结束并完成任务
-> 查看 25 日复盘 -> 执行顺延
=> 25 日仍保留完成记录，26 日不出现这条完成任务。
```

### 场景 B：两位朋友安静共同专注

```text
甲乙已是好友且加入同一房间 -> 甲选择该房间为当前 -> 发起共同专注
-> 乙确认 -> 两人各自开始计时 -> 乙暂停 -> 甲继续
-> 乙恢复，甲自然结束 -> 乙稍后结束
=> 两人的任务和时长独立；房间只显示各自状态和最终摘要；没有人被远程暂停。
```

### 场景 C：一个用户有多个房间和多个朋友

```text
甲加入 A、B、C 三个房间，A 为当前；小窗勾选 A 的两名朋友
-> 甲浏览 B 的消息 -> 回到今天页继续专注
=> 小窗仍只显示 A 中被勾选的朋友；浏览 B 不改甲的 Presence 或计时。
```

### 场景 D：自定义角色安全同步

```text
甲在本地导入并预览 AI 小猫 -> 显式授权给当前房间 -> 上传 A@3
-> 乙看到资源元数据 -> 当该猫进入可见座位时下载 atlas
=> 乙先校验哈希再显示；甲撤销授权后乙不再发起下载；所有失败回退默认猫，不影响倒计时。
```

---

## 18. 当前差距与下一步

截至 v0.1.20，项目拥有可用的本地核心和陪伴演示，但陪伴仍是本地模拟，不能声称已具备真实好友同步。本轮已完成本地任务历史、完成归档、实际专注时长与计划视图的首轮回归；后续仍按以下顺序推进：

1. **v0.1.21：把演示好友状态重构为本地 Room/Presence 模型。** 这是未来实时功能的地基；
2. **v0.1.22：实现离线 JJPET-Lite 导入和统一场景渲染。** 不触网，先验证体验和性能；
3. **v0.2.x：再接账号、好友、房间和资源服务。** 每一类数据按第 12 节独立同步，避免把个人 Todo 放上云。

本文是实施依据；每一个版本还应写一个小范围的变更 Spec，包含 Given/When/Then 场景、涉及文件、测试和回滚条件，不能直接跨越本规格进入大面积修改。

## 资料来源

- `package.json`：当前版本、Electron/React/Vite 技术基线和 Windows 构建方式。
- `README.md`：已实现的本地功能、启动方式和现有回归范围。
- `JJtomato-latest-product-solution.md`：任务、专注、陪伴、隐私和性能的产品方向。
- `JJtomato-market-research-and-roadmap.md`：轻量化和日/周/月计划视图的取舍背景。
- `JJtomato-study-room-avatar-spec-v0.1.md`：JJPET-Lite 与形象系统的详细资源规范。
