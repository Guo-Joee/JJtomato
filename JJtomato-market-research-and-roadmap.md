# JJtomato 市场调研与产品路线方案

> 调研日期：2026-08-27
>
> 目标：在不把 JJtomato 做成臃肿的“全能效率套件”的前提下，找到日常 Todo、计划、专注和朋友监督之间的最小有效闭环。

## 结论先行

JJtomato 不应该和 Todoist、滴答清单比“谁的功能更多”。它更适合占据一个清晰的细分位置：

> **给老电脑和低干扰学习场景使用的本地优先专注 Todo：把今天要做的下一步，和真实专注记录、历史进展以及熟人陪伴连接起来。**

建议坚持四个产品原则：

1. **今天优先**：打开软件后先看到今天真正要做的事，而不是项目管理后台。
2. **任务和专注绑定**：每次番茄都对应一个具体任务，完成后留下真实记录。
3. **监督不做监控**：好友看到的是目标、状态和完成结果，不是摄像头、屏幕录制或键盘监控。
4. **本地优先、联网可选**：断网时任务、计时和历史照常可用；分享和同步是增量能力。

## 一、当前项目判断

当前 JJtomato 已经有一个适合继续演进的雏形：

- 技术栈是 Electron + React + Vite；
- 任务、子任务、番茄计时、暂停、跨日历史、实际专注时长和日历式 Todo 已经存在；
- 当前依赖主要是 React、Electron、Vite、lucide-react 和 motion，没有业务运行时后端依赖；
- 数据主要保存在本机，适合先把单机体验做稳定，再增加可选同步；
- Windows 0.1.17 安装包约 82 MB。包体积不等于运行内存，但说明继续堆叠大型功能和资源需要谨慎。

当前最重要的不是继续增加页面，而是补齐这条路径：

```text
快速记录 → 选出今天最重要的下一步 → 开始专注
       → 被打断后恢复 → 完成并记录实际时长
       → 晚间复盘 → 与朋友分享轻量进展
```

## 二、市场格局与可借鉴点

| 产品 | 已经解决得好的问题 | 对 JJtomato 的启发 | 不建议照搬的部分 |
|---|---|---|---|
| Microsoft To Do | “My Day”把注意力收回到今天；未完成任务回到原列表并在次日建议；支持共享列表和分配任务。[官方说明](https://support.microsoft.com/en-us/ToDo/my-day-and-suggestions) [共享列表](https://support.microsoft.com/en-us/ToDo/share-a-task-list) | “今天”应该是一个清爽的执行空间，而不是永久堆积的任务仓库 | 依赖账号和云端，专注计时不是核心 |
| Todoist | 项目、优先级、标签、描述、子任务和灵活视图；共享项目支持分配、评论和动态。[功能页](https://www.todoist.com/features) [任务视图](https://www.todoist.com/help/articles/use-the-task-view-to-manage-tasks-in-todoist-eDeRDO0C) | 任务详情应按需展开；协作要有明确负责人和上下文 | 不要一开始就做成通用项目管理平台 |
| 滴答清单 TickTick | 把清单、日历、时间线、番茄、习惯、统计、共享协作和快捷添加放在一个产品里；官方还强调桌面端“计划与执行一屏掌握”。[功能页](https://ticktick.com/features?language=zh_CN) [Windows 版](https://ticktick.com/windows?language=zh_CN) | 用户确实需要“计划”和“执行”联动；快捷添加、日历拖拽和专注小窗值得借鉴 | 它已经覆盖广泛全场景，JJtomato 不应复制它的功能广度 |
| Forest | 用可见的树成长记录专注，支持 Plant Together、专注统计、阻断干扰和离线核心体验。[官方说明](https://www.forestapp.cc/) | 进步应该可见；好友共同专注可以有一个简单、即时的共同结果 | 不要依赖惩罚、死亡树或复杂收藏系统来维持使用 |
| Focusmate | 用户预约 25/50/75 分钟，与伙伴配对，开场说目标，结束报告结果；核心是 body doubling 和轻量承诺。[工作方式](https://support.focusmate.com/en/articles/9110188-getting-started) [官方主页](https://www.focusmate.com/) | “开始前声明目标 + 结束后回报结果”比排行榜更适合互相监督 | Focusmate 以实时视频和陌生人匹配为核心，JJtomato 不应把视频会议塞进桌面 Todo |
| Super Productivity | 本地优先、隐私导向、离线可用，并提供时间追踪、番茄和外部同步选项。[官方仓库说明](https://github.com/super-productivity/super-productivity) [同步文档](https://github.com/super-productivity/super-productivity/blob/master/docs/wiki/1.02-Configure-Data-Synchronization.md) | 本地数据、导出、可选同步和透明隐私是轻量产品的可信基础 | 插件、开发者集成和大量配置会把普通用户带入复杂工作流 |

### 市场空位

目前几类产品各自偏科：

- Todo 应用擅长整理任务，但专注过程通常只是附加功能；
- 专注应用擅长启动和激励，但任务计划与历史回顾较弱；
- body doubling 产品擅长陪伴，但不是个人 Todo 系统；
- 本地优先产品重视隐私和控制，但往往学习成本较高；
- 大型产品拥有完整协作，却带来账号、网络、通知和界面复杂度。

JJtomato 的机会是把四件事做得足够好：**今天的下一步、真实专注记录、跨日不丢历史、熟人之间的轻监督**。

## 三、推荐产品定位

### 核心用户

第一阶段只服务三类人：

1. 在 Windows 老电脑上学习、备考或写代码的人；
2. 需要把大任务拆成下一步，但不想维护复杂项目系统的人；
3. 想和一两个朋友一起学习，却不想开视频会议或加入陌生人社区的人。

### 一句话价值

> **不是帮你管理所有事情，而是陪你把今天的下一步做完。**

### 与竞品的差异化承诺

```text
本地可用       断网也能计时、记录、复盘
轻量           不内置聊天、视频、AI大模型和社交信息流
可追溯         顺延不覆盖历史，完成记录按日期保留
强绑定         每颗番茄都能知道在做哪一个任务
轻监督         朋友只看约定、状态和结果，不监控设备
```

## 四、功能路线：按闭环而不是按模块增加

### P0：单机核心可靠性

这是下一阶段的必做项，目标是让用户每天愿意打开：

- **今日页**：快速添加、拖动排序、设置今天/未来日期、优先级和预计番茄数；
- **渐进式任务详情**：创建时只填名称，点击“更多”再填日期、备注、子任务和预计番茄；
- **当前任务**：开始番茄前明确选择任务，计时器上显示任务名和子任务；
- **暂停与中断恢复**：暂停不等于失败，要求可选原因或一句备注，恢复时回到同一任务；
- **真实记录**：保存开始时间、结束时间、实际专注秒数、暂停次数和完成状态；
- **跨日规则**：未完成任务顺延，已完成任务只留在原日期；每个日期都是不可被后续操作覆盖的历史快照；
- **备份与恢复**：导出 JSON、Markdown，导入前预览并自动生成本地备份；
- **空状态和错误恢复**：首次使用给出一个 5 分钟示例任务，数据损坏时允许从最近备份恢复。

P0 的验收标准：

- 从新建任务到开始第一颗番茄不超过 3 个主要操作；
- 任务暂停、恢复、完成、顺延和跨日历史都有自动化回归测试；
- 断网时核心流程不受影响；
- 任务数量和历史天数增加后，界面仍可滚动且不出现重叠、丢失或重复记录。

### P1：每日计划和复盘

- **早晨计划**：只要求用户选出 1 个“必须完成”和最多 3 个“如果有时间”；
- **智能分组**：今天、逾期、未来、已完成，不再把所有内容塞在一个列表；
- **日视图**：执行清单，突出当前任务和剩余番茄；
- **周视图**：按日期分栏的计划板，只有有明确时段时才显示时间块；
- **月视图**：只展示完成密度、专注小时和延期趋势，用于回顾而非编辑；
- **每日复盘**：完成了什么、什么被打断、未完成原因、明天第一步；
- **真实进度**：按任务展示计划番茄、实际分钟、暂停次数和完成日期。

日历视图建议遵守一个简单规则：

> **没有开始/结束时间的任务，不画甘特条；它只是一张日期任务卡。**

这样可以避免把 Todo 伪装成项目管理甘特图，也能减少用户误以为必须精确安排每一分钟的压力。

### P2：朋友监督 MVP

第一版不要做公开社区，也不要做视频。做一个“熟人专注房间”：

1. 创建房间，设置名称、时长和本次目标；
2. 通过一次性邀请链接邀请 1～4 位朋友；
3. 每人只看到自己的任务详情，以及其他成员的昵称、准备/专注/暂停/完成状态；
4. 开始时每个人写一句“这段时间要完成什么”；
5. 结束时每个人选择“完成 / 部分完成 / 被打断”，可补一句备注；
6. 房间生成一张简洁结果卡：总专注时长、完成任务数、共同完成率；
7. 房间默认在结束后只保留摘要，原始任务仍属于各自本地数据。

这里的关键设计是：**共享的是承诺和结果，不是完整的私人任务数据库**。这样既减轻同步冲突，也保护隐私。

建议加入但默认关闭的轻提醒：

- 朋友开始专注时提示“某某已开始”；
- 长时间暂停时提示“要不要回来继续”；
- 结束前 1 分钟提示准备回报；
- 只允许房间内手动发送有限的鼓励语，不做聊天时间线。

明确不做：摄像头、屏幕录制、键盘/鼠标监控、公开排名、陌生人匹配、惩罚性扣分。

### P3：可选同步和 AI

#### 同步

- 本地数据永远是可用的主路径；
- 登录和云同步由用户主动开启；
- 个人任务同步可以采用版本号 + 事件记录，冲突时保留两个版本而不是静默覆盖；
- 房间状态使用短轮询或轻量 Server-Sent Events，第一版不引入复杂实时协作框架；
- 用户可以随时导出全部数据并删除云端副本。

#### AI

AI 只做“减少输入成本”的按钮，不做后台常驻助手：

- “把这个大任务拆成 3 个下一步”；
- “根据今天剩余时间帮我挑 1～3 个任务”；
- “把复盘整理成 Markdown”；
- “根据任务生成一个 5 分钟起步动作”。

默认先使用本地规则完成日期识别、番茄估算和基础拆解；AI 调用必须明确显示发送了什么内容、调用哪个服务，并允许关闭。不要把大模型、模型文件或向量数据库打进安装包。

## 五、轻量化技术路线

### 现在不建议立刻迁移到 Tauri

Electron 官方明确说明其继承 Chromium 的多进程架构，每个 `BrowserWindow` 会有单独的 renderer 进程；这带来稳定性和兼容性，也带来运行时开销。[Electron 进程模型](https://www.electronjs.org/docs/latest/tutorial/process-model)

Tauri 的优势是使用系统 WebView，不必为每个应用捆绑浏览器引擎，官方将其定位为更小的应用包。[Tauri 官方说明](https://tauri.app/start/)

但 JJtomato 目前更大的风险是功能和数据规则仍在快速变化。推荐：

1. v0.x 继续使用 Electron，先把行为契约、历史数据和协作模型稳定；
2. 在低端 Windows 测试机上记录真实启动时间、空闲 RSS、专注时 CPU 和 8 小时内存增长；
3. 只有当数据证明 Electron 达不到预算，再评估 Tauri 迁移；
4. 迁移必须保持数据格式兼容，并先做一个只读原型，不要一次性重写全部桌面能力。

### 性能预算建议

这些是开发验收目标，不是对当前版本的测量结论：

| 指标 | 建议目标 |
|---|---|
| 老电脑冷启动到可操作 | ≤ 2.5 秒 |
| 主窗口空闲内存 | ≤ 150 MB RSS，需用实际机器测量 |
| 计时运行时 CPU | 空闲时平均 ≤ 1% |
| 连续运行 8 小时 | 内存增长 ≤ 10 MB，不能持续增长 |
| 无网络模式 | 计时、任务、历史和导出完全可用 |
| 历史数据 | 默认只加载当前范围，月度历史按需加载 |

实现原则：

- 只保留一个主窗口；小窗按需创建，关闭后销毁；
- 计时器保存 deadline，不依赖高频重渲染；
- 本地数据从 localStorage 逐步迁移到 IndexedDB 或小型本地数据库，避免历史越来越大时反复同步整份 JSON；
- 日历按范围加载，超过 90 天的历史不一次性渲染；
- 动画、背景光效和白噪音都提供低功耗开关；
- 不在启动时加载 AI SDK、图表大包、视频组件或聊天组件；
- 远程请求统一放在明确的同步动作中，离线时不重试、不常驻轮询；
- Electron 主进程避免同步磁盘 I/O，遵循官方性能建议。[Electron 性能指南](https://www.electronjs.org/docs/latest/tutorial/performance)

## 六、界面和交互逻辑建议

### 顶层导航只保留三个入口

```text
今天       现在要做什么、开始哪颗番茄
计划       今天/本周/本月如何安排
复盘       已完成什么、为什么拖延、下一步是什么
```

好友房间放在“今天”页的次级入口，不再新增一个长期占据空间的社交主导航。

### 创建任务采用渐进式披露

第一步只有一个输入框：`接下来要做什么？`

创建后卡片上直接显示：

```text
任务名       [开始专注]
今天         预计 1 个番茄
```

点击“更多”才显示日期、优先级、子任务、备注和提醒。任务名过长时保持两行省略，点击展开，详情面板再显示完整文本。所有展开必须让卡片自然撑高或进入内部滚动，不能用绝对定位覆盖下一项。

### 操作命名要符合直觉

| 当前状态 | 主按钮 | 次级操作 |
|---|---|---|
| 未开始 | 开始专注 | 编辑、顺延、拆分 |
| 专注中 | 暂停 | 结束本次、切换任务 |
| 已暂停 | 继续专注 | 记录中断原因 |
| 完成 | 查看记录 | 撤销完成 |
| 过期未完成 | 今天继续 | 改期、放弃 |

不要让用户通过右键才能发现“创建子任务”这类高频功能；右键可以保留，但必须同时提供可见按钮或详情面板入口。

## 七、数据模型建议

将“任务定义”“每日计划实例”“专注记录”“好友房间事件”分开，避免一个任务对象承担所有历史：

```text
Task                  长期任务定义、名称、父子关系
TaskPlan              某天是否计划、预计番茄、状态
FocusSession          开始/结束/实际秒数/暂停原因/关联 TaskPlan
DailyReview           某天的完成、阻塞、明日第一步
RoomSession           房间、成员、开始结束、共同目标
RoomEvent             加入、开始、暂停、完成、回报结果
```

这样可以同时满足：

- 任务改名不会改写过去的日报文字；
- 已完成任务不会被顺延到第二天；
- 同一任务可以在不同日期有不同计划实例；
- 房间只同步状态事件，不同步用户全部私密数据；
- 未来切换存储方式或同步服务时，旧数据仍能导入。

## 八、指标与判断标准

北极星指标建议继续使用：

> **每周完成的、与具体任务绑定的有效专注时长，以及由此完成的真实任务数。**

不要把打开次数、通知点击次数、积分或连续天数作为主要成功标准。

需要持续观察的指标：

- 首次打开到第一颗番茄开始的完成率；
- 第一颗番茄完成率；
- 暂停后 30 分钟内恢复率；
- 任务完成后再次创建下一步的比例；
- 顺延任务的历史查看率；
- 日复盘完成率；
- 邀请好友后首次共同专注完成率；
- 房间结束时双方回报率；
- 低端机启动时间、空闲 RSS、CPU 和长时间运行内存曲线。

如果某功能不能改善“开始、继续、完成、复盘、陪伴”中的至少一个环节，就暂缓。

## 九、明确暂缓的功能

为了守住轻量定位，以下功能在 v1.0 前不做或只保留外部链接：

- 内置聊天、语音和视频会议；
- 公开社区、陌生人匹配和排行榜；
- 常驻 AI 聊天窗口；
- 本地大模型和模型下载器；
- 附件、图片库、知识库、白板和复杂笔记；
- 全量日历集成、邮件解析和多平台插件生态；
- 复杂依赖图、资源管理和多人项目权限；
- 为了“留存”而加入的签到惩罚、无限徽章和信息流。

## 十、推荐版本顺序

```text
v0.2  核心可靠性：任务详情、当前任务、暂停原因、备份恢复、历史数据模型
v0.3  每日闭环：早晨计划、执行/计划/复盘三视图、复盘卡片、真实进度
v0.4  熟人陪伴：1～4 人专注房间、目标声明、状态同步、结束回报、结果卡
v0.5  可选同步：账号、跨设备、事件版本、冲突保留、隐私开关
v0.6  AI 按钮：任务拆解、今日选择、复盘整理，默认关闭且不常驻
v1.0  性能与发布：低端机基准、稳定迁移、数据导入导出、更新回滚、无障碍和多语言
```

最终建议：先把 JJtomato 做成一个“每天真的能用、断网也可靠、朋友愿意一起打开”的小工具，再考虑 Tauri、AI、公开社区和复杂同步。轻量不是少做功能，而是只保留能够让用户开始并完成下一步的功能。
