# JJtomato 自习室与统一陪伴形象规格

> 文档版本：v0.1（设计冻结前草案）<br>
> 日期：2026-08-27<br>
> 对应产品阶段：v0.2.0「真实实时陪伴」之后的形象系统<br>
> 本文性质：可直接拆分开发任务的规格；不代表当前本地演示版已经具备联网、多人房间或资源上传能力。

## 0. 结论与边界

JJtomato 要有两个陪伴画面，但**只有一套角色形象和一份状态**：

```text
用户资料中的 avatarRef ─┐
房间的实时 Presence ─────┼─> AvatarRenderer（统一渲染器）
当前激活房间 / 计时状态 ─┘
                             ├─> 主页面：自习室空间
                             └─> 置顶小窗：桌边陪伴
```

- 主页面的“自习室空间”是一个固定场景：成员坐在固定坐垫/座位上，最多展示 8 人；多于 8 人时显示“还有 N 位在场”，不挤出小动物墙。
- 置顶小窗是另一个固定场景：倒计时为中心，周围最多展示 4 位被用户勾选的朋友；每多一位朋友，多一只简化的小动物，不出现整张教室背景。
- 两处都使用同一个 `avatarId + revision` 和同一个 `Presence`。场景只决定座位、道具、缩放与是否降低帧率，**绝不拥有第二套角色或第二份状态**。
- 角色包只包含“小动物本体动画”。桌垫、电脑、咖啡、键盘、书本、倒计时、状态气泡等属于软件场景资产，由 JJtomato 固定提供。这样猫、狗、番茄或植物都能放进同一张桌边/自习室，而且不会因为用户自制图带背景而难以兼容。

本期不做视频、语音、屏幕/应用名同步、按键内容上传、图片聊天、开放广场或陌生人匹配。全局键盘监听即使以后开启，也只能产生 `typing / idle` 两种状态，绝不能成为录入按键的通道。

## 1. 名词与不变量

| 名词 | 含义 | 不能混淆为 |
| --- | --- | --- |
| 形象（Avatar） | 用户选择的一只小动物及其动画资源 | 好友在线状态、房间成员资格 |
| 形象版本（revision） | 同一形象的不可变资源版本 | 可被原地覆盖的图片文件 |
| 自习室（Room） | 一组好友共同查看状态、消息和互动的空间 | 当前倒计时本身 |
| 当前自习室（active room） | 唯一能驱动本人计时器和置顶小窗的房间 | 用户浏览中的任意房间 |
| Presence | 某位成员当前的在线/专注/休息等实时状态 | 历史专注记录 |
| 场景（Scene） | 主页面自习室或桌边小窗的固定背景与座位规则 | 用户自定义形象包 |
| 资源包（`.jjtpet`） | 可导入、可导出的离线形象压缩包 | 服务器上好友身份或权限 |

必须长期保持的规则：

1. 一位用户任何时刻只拥有一个 `activeRoomId`；她可以浏览多个房间，但不能让两个房间同时控制自己的番茄钟。
2. `Presence` 由真实事件推导，不能靠自由文本“看起来像在专注”。例如“和你一起专注”是协作关系，不是 `activity` 字段的一个随意字符串。
3. 已上传的 `avatarId@revision` 永不改写；改图即创建新 revision。旧客户端和离线缓存永远可继续使用旧版本。
4. 资源同步与状态同步分离。在线状态是极小的实时事件；图像走 CDN/对象存储的按需下载，不能通过 WebSocket 反复传图。
5. 退出、暂停、重置、切换番茄模式、自然结束、离开房间，都必须通过同一个会话收尾函数处理，不能只改界面文案。

## 2. 房间、状态与场景的唯一数据源

### 2.1 最小数据模型

```ts
type AvatarRef = {
  avatarId: string;          // 例如 cat-mochi，服务端分配/校验
  revision: number;          // 正整数，资源不可变
  sha256: string;            // atlas.webp 的 SHA-256，64 位小写 hex
  previewSha256: string;
};

type PresenceActivity =
  | 'offline'
  | 'online'
  | 'ready'
  | 'focusing'
  | 'typing'
  | 'browsing'
  | 'short_rest'
  | 'long_rest';

type Presence = {
  memberId: string;
  roomId: string;
  activity: PresenceActivity;
  focusPhase: 'none' | 'work' | 'short_break' | 'long_break';
  focusSessionId: string | null;
  sharedFocusMemberIds: string[]; // 只表示本轮明确确认的共同专注成员
  stateVersion: number;           // 同一 memberId 单调递增，丢弃旧事件
  occurredAt: string;             // ISO 8601 UTC
  expiresAt: string;              // 心跳失效时间；过期显示 away/offline
};

type RoomMember = {
  roomId: string;
  memberId: string;
  role: 'owner' | 'member';
  seatPreference: number | null;  // 仅偏好，不保证永远占位
  edgeVisible: boolean;           // 此朋友是否允许显示于本人的小窗
  avatarRef: AvatarRef | null;    // null 时使用系统默认形象
};
```

`Task`、`TaskPlan` 和 `FocusSession` 继续是个人本地优先数据；房间只能得到用户显式共享的“状态摘要”和最终时长，不可读取完整待办列表。需要显示当前任务时，另设 `sharedTaskTitle` 且默认 `null`，不能把本地任务标题自动上传。

### 2.2 活动优先级与冲突消解

一个人可能同时在专注、打字、浏览网页。为了不让朋友看到状态抖动，客户端在本机先计算，再发送一个最终状态：

| 条件（从高到低） | 对外 `activity` | 角色动作 |
| --- | --- | --- |
| 未登录、心跳过期、明确隐身 | `offline` | idle 的睡眠/静止降级画面 |
| 番茄处于长休息 | `long_rest` | 喝咖啡/放松 |
| 番茄处于短休息 | `short_rest` | 瘫在坐垫或伸懒腰 |
| 番茄正在运行且 3 秒内有输入 | `typing` | 小爪敲键盘 |
| 番茄正在运行且浏览状态授权且活跃 | `browsing` | 看亮着的电脑屏幕 |
| 番茄正在运行 | `focusing` | 安静看电脑/书 |
| 已入房、未开番茄 | `ready` | 坐好准备开始 |
| 在线但不在当前活动 | `online` | idle |

说明：`typing` 是 `focusing` 的视觉子状态，不能另开一份计时；`browsing` 只在用户主动授予“浏览状态”后才可用，首版可完全不实现；全局输入原始事件不写文件、不进日志，仅在 2–3 秒无输入后从 `typing` 回退到其底层专注状态。

### 2.3 多房间规则

用户可以创建或加入多个自习室，但只有一个“当前自习室”。

```text
浏览房间 B ──> 只读查看成员、消息、座位
点击“设为当前” ──> 若本人未专注：切换 activeRoomId
                    若本人专注：提示「结束本轮后切换」或「暂停并切换」
```

- 专注中不自动切换当前房间；网络重连、好友邀请或收到消息都不能改变 `activeRoomId`。
- “和朋友一起专注”先创建/加入共同 `focusSessionId`，再写入双方（或多方）的 `sharedFocusMemberIds`；任何成员暂停只改变自己的 Presence，不把其他人强制暂停。
- 本人重置、跳过、切换工作/休息、自然结束或离开当前房间时，统一清理本人 `focusSessionId` 和 `sharedFocusMemberIds`。其他成员保留自己的会话，UI 文案变成“她已离开本轮”。
- 当前 `src/renderer/App.jsx` 的单个 `togetherCompanionId` 仅适合本地演示；真实房间开发前必须迁移为上述 `activeRoomId + focusSessionId + memberIds`，不能直接扩展这个字符串。

## 3. 两个场景的渲染规格

### 3.1 主页面：自习室空间

入口位于“今天”页的陪伴区，可折叠，不抢占今日任务和计时器。初始画面只显示当前房间的成员，不显示陌生人。

```text
┌──────────────── 自习室 · 3 人在场 ────────────────┐
│  [座位1] 小猫  状态气泡     [座位2] 小狗 状态气泡 │
│                                                    │
│       [座位3] 番茄  状态气泡   [+2 位在场]         │
│                                                    │
│  你正在：25:00  │  [切换当前自习室] [打开聊天]    │
└────────────────────────────────────────────────────┘
```

- 固定背景一张静态 WebP/PNG；角色依座位锚点渲染，不给每个用户生成背景。
- 常规布局为 2×4 共 8 个座位。座位按“自己优先、正在共同专注者、其余在线、离线成员”排序，避免用户把自己的朋友找半天。
- 角色显示高度：主场景 56–76 CSS px；状态气泡最多 12 个中文字符，超出省略且悬停显示完整提示。
- 8 人之外仅展示头像预览栈和数量，不渲染完整 atlas；点击后可打开成员列表。
- 对低性能设备，非当前用户/非共同专注成员使用 `preview.webp` 静态图；最多 4 个全动画角色。

### 3.2 置顶小窗：桌边陪伴

现有番茄小窗继续以倒计时为第一优先级。陪伴形象是可选附加层，设置中逐个朋友开关，默认全部关闭，首次开启只推荐勾选 1 人。

| 项目 | 规格 |
| --- | --- |
| 可见人数 | 0–4 人；超过时显示 `+N`，不缩小到不可读 |
| 角色高度 | 24–40 CSS px；随窗口尺寸分级，不使用任意缩放 |
| 窗口 | 透明、无完整教室背景；倒计时中心，角色环绕底部/两侧 |
| 状态 | 只显示小色点/极短状态（如“专注”“休息”）；完整文本放悬停卡片 |
| 道具 | 电脑、键盘、咖啡、坐垫由桌边场景绘制，按状态替换 |
| 点击 | 点击角色打开该好友的陪伴面板；点击倒计时保持现有控制逻辑 |
| 无障碍/省电 | `prefers-reduced-motion`、窗口隐藏或最小化时冻结到单帧 |

桌边布局采用“多一位朋友，多一只小动物”的策略：每个角色坐在自己的小垫子上；当她 `typing` 时在面前出现统一的小键盘，`browsing` 时电脑屏幕亮起，短休/长休换成固定的休息道具。角色图里禁止携带电脑、桌子或文字，否则换场景会冲突。

### 3.3 同步原则与 IPC

- React 主页面和 mini BrowserWindow 都从同一个 `CompanionStore` 订阅已归一化的 `RoomSnapshot`。
- Electron 主进程仍是番茄 deadline 的唯一计时权威；渲染进程不得因宠物动画暂停而停止计时。
- mini 窗口 IPC 只接收已过滤的 `activeRoomId`、倒计时摘要、最多 4 位的 `avatarRef + activity + displayName + stateVersion`。它不读取完整聊天、任务、历史或键盘事件。
- 收到更高 `stateVersion` 后两个场景在同一批状态更新中换动作；资源文件未下载完成时保留旧帧或用默认形象，不显示破图。

## 4. JJPET-Lite v1：用户形象包格式

JJPET-Lite 是 JJtomato 自己的轻量格式，和其他软件的宠物包不互通。它的目标是“能放进固定场景的小角色”，不是制作独立游戏动画。

### 4.1 最终包和精确文件名

用户导入、导出、好友私下传递的最终文件扩展名为 **`.jjtpet`**。它是 ZIP 容器，内部文件名必须完全一致：

```text
my-avatar.jjtpet
├─ manifest.json        必需，UTF-8，无 BOM，最大 16 KB
├─ preview.webp         必需，256 × 256，透明背景，最大 80 KB
└─ atlas.webp           必需，1024 × 960，透明背景，最大 1.20 MB
```

严禁包含 `.exe`、`.dll`、`.js`、`.html`、`.svg`、字体、视频、GIF、音频、链接文件、软链接、目录穿越路径或其他额外文件。导入器发现额外文件即拒绝，不“猜测执行”。ZIP 解压后的总大小不得超过 1.5 MB，防止压缩炸弹。

| 文件 | 用途 | 软件如何使用 |
| --- | --- | --- |
| `manifest.json` | 描述名称、网格、动作帧率与兼容版本 | 导入时先校验，不直接信任显示文本 |
| `preview.webp` | 好友列表、离线缓存、低性能静态回退 | 未下载 atlas / 背景成员时优先使用 |
| `atlas.webp` | 统一动画图集 | 仅在角色可见且允许动画时解码 |

### 4.2 图集格子与动作行

`atlas.webp` 固定为 8 列 × 6 行，共 48 个 128 × 160 px 的格子：

```text
总尺寸 1024 × 960（8 × 128，6 × 160）

第 0 行  idle        帧 0–5 有效，6–7 必须完全透明，3 FPS
第 1 行  focusing    帧 0–7 有效，6 FPS
第 2 行  typing      帧 0–7 有效，8 FPS
第 3 行  browsing    帧 0–7 有效，5 FPS
第 4 行  short_rest  帧 0–5 有效，6–7 必须完全透明，4 FPS
第 5 行  long_rest   帧 0–7 有效，6 FPS
```

所有行循环播放。软件按 `frameCount` 取帧，透明的预留格必须没有任何残留像素。`typing`、`browsing`、`short_rest`、`long_rest` 不适合或缺失时不降级为报错，而按下表安静回退：

```text
typing      -> focusing
browsing    -> focusing
short_rest  -> idle
long_rest   -> short_rest -> idle
offline     -> idle 的第 0 帧 + 场景睡眠遮罩
ready/online-> idle
```

> 选用 6–8 帧而非两帧，是为了让呼吸、敲键盘、休息等动作有可读性；又不采用每个动作几十帧，避免多人同时显示时占用过高。

### 4.3 姿态合同（Pose Contract）

v1 仅支持 `seated-front-v1`，即正面或轻微 3/4 正面的坐姿小动物。所有角色不论猫、狗、兔子、番茄或植物，都遵循同一占位合同：

| 项目 | 强制规则 |
| --- | --- |
| 基线 | 每格角色接触坐垫的基线为 `y=148 ± 2 px` |
| 安全边距 | 非透明像素须在 `x=8..120`、`y=12..154` 内；不得触碰格子边缘 |
| 重心 | 身体中心尽量在 `x=64 ± 20 px`；禁止左右大跳或缩放跳动 |
| 朝向 | 正面或向右不超过 15° 的轻微侧身；禁止背面、俯视、全侧脸 |
| 图层 | 角色本体在前；场景会在其后放坐垫/显示器，在其前放小键盘 |
| 禁止内容 | 背景、桌子、电脑、键盘、书、咖啡、文字、数字、头像框、聊天气泡、logo、水印、投影、发光特效、悬浮符号 |
| 连贯性 | 六行角色必须是同一只角色：脸、花纹、颜色、材质、比例和剪影不能漂移 |

角色需要表达状态时只能通过眼睛、耳朵、爪子、身体小幅移动实现；不要画“正在输入”“休息中”等文字。所有语义由场景和 UI 文案提供，因此更容易翻译和更节省资源。

### 4.4 `manifest.json` 完整示例

```json
{
  "specVersion": "jjtpet-lite/1",
  "id": "cat-mochi",
  "revision": 1,
  "displayName": "糯米小猫",
  "kind": "animal",
  "poseProfile": "seated-front-v1",
  "assets": {
    "preview": "preview.webp",
    "atlas": "atlas.webp"
  },
  "atlas": {
    "columns": 8,
    "rows": 6,
    "cellWidth": 128,
    "cellHeight": 160,
    "width": 1024,
    "height": 960
  },
  "animations": {
    "idle":       { "row": 0, "frameCount": 6, "fps": 3, "loop": true },
    "focusing":   { "row": 1, "frameCount": 8, "fps": 6, "loop": true },
    "typing":     { "row": 2, "frameCount": 8, "fps": 8, "loop": true },
    "browsing":   { "row": 3, "frameCount": 8, "fps": 5, "loop": true },
    "short_rest": { "row": 4, "frameCount": 6, "fps": 4, "loop": true },
    "long_rest":  { "row": 5, "frameCount": 8, "fps": 6, "loop": true }
  },
  "fallbacks": {
    "typing": "focusing",
    "browsing": "focusing",
    "short_rest": "idle",
    "long_rest": "short_rest"
  },
  "license": "personal-use",
  "createdWith": "JJtomato Avatar Kit v1"
}
```

校验规则：

- `id` 必须匹配 `^[a-z0-9][a-z0-9-]{2,39}$`，不由昵称生成；同一账号创建时服务端再保证唯一。
- `displayName` 为 1–24 个字符，只用于本地显示；不作为文件路径、SQL、HTML 或权限判断。
- `kind` 仅允许 `animal`、`plant`、`food`、`other`；不影响渲染。
- `revision` 在离线包可为 `1`，上传时由服务端分配递增值，客户端不能自行覆盖线上版本。
- v1 必须包含六行动作；将来如果允许精简包，必须升级到新的 `specVersion`，不能在 v1 静默缺行。

## 5. 给用户的 AI 创建流程

AI 很难一次可靠地产出精准的 48 格图集。因此软件必须提供“创建套件 + 导入检查 + 预览”流程；用户不需要手写 JSON，也不需要知道坐标。

### 5.1 软件内的操作步骤

1. 打开 **设置 → 我的形象 → 创建新形象**，填写昵称、角色类型（猫/狗/植物等）和风格关键词。
2. 点击 **下载 AI 创建套件**。软件导出一个本地文件夹（或 ZIP），名称固定为：

   ```text
   jj-avatar-kit-<avatar-id>/
   ├─ README-给AI.md
   ├─ manifest.template.json
   ├─ layout-guide.png
   ├─ avatar-preview.png.example
   └─ avatar-atlas.png.example
   ```

3. 用户把 `README-给AI.md` 和 `layout-guide.png` 一并交给图像 AI；如果已有自己画的基础形象，也一并上传作参考图。
4. AI 产出阶段只接受两个工作文件，名字必须完全一致：

   ```text
   avatar-preview.png   // 256 × 256，透明背景
   avatar-atlas.png     // 1024 × 960，透明背景，8 × 6 图集
   ```

5. 回到 JJtomato，点击 **导入 AI 结果**，选择这两个 PNG 所在的文件夹，或直接拖入两个文件。导入器负责转为 WebP、生成 `manifest.json`、跑尺寸和视觉完整性检查。
6. 软件展示“好友列表 / 自习室 / 置顶小窗”三个预览，用户逐项播放 6 个动作。只有点击 **保存为我的形象** 才写入本机资源库。
7. 默认仅本机可见。用户另行点 **分享给好友** 才进入上传流程；取消或关闭窗口不会上传任何文件。

### 5.2 `README-给AI.md` 应包含的可复制提示词

创建套件内必须生成下列内容（用户只需把中括号替换为自己的想法）：

```text
请为 JJtomato 生成同一只 [角色，例如：橘白小猫] 的透明背景动画图集。

严格输出一张 PNG：文件名 avatar-atlas.png，画布固定 1024×960 像素，8 列×6 行，
每格 128×160 像素。不要输出说明图、分镜图、白底图、网格、文字或水印。

这是一只坐在小垫子上的正面/轻微右转小角色：所有格子的脚/身体底部都落在每格 y=148 附近，
角色不要超出每格 x=8..120、y=12..154。保持同一只角色的脸、花纹、配色、材质、比例和画风。
角色图中不能有电脑、桌子、键盘、书本、咖啡、背景、聊天气泡、数字或状态文字；这些由软件场景添加。

第0行：idle，前6格是平静呼吸、眨眼、耳朵微动，后2格完全透明。
第1行：focusing，8格是安静专注、看前方屏幕/书的细微动作。
第2行：typing，8格是双爪在身前轻快敲键盘的动作；不要画键盘本体。
第3行：browsing，8格是看屏幕、轻微左右浏览的动作；不要画屏幕本体。
第4行：short_rest，前6格是瘫坐、伸懒腰或放松，后2格完全透明。
第5行：long_rest，8格是更放松、喝水前的等待姿态；不要画杯子本体。

画面必须透明背景，格子之间必须留空，不能让角色跨格；禁止阴影、发光、拖影、速度线、漂浮星星、
文字、logo、签名或水印。先确保每一格都是完整同一角色，再保证动作变化。
```

另外，套件中的 `README-给AI.md` 要明确提醒用户：

- 最好先用 AI 生成一张“角色设定图”，确认角色样貌后，把它作为所有动作生成的参考图；不要每一行都重新描述一种角色。
- 如果 AI 很难一次生成整张图集，推荐按“6 行横向动作条”分别生成，但每行都附上同一张设定图和 `layout-guide.png`；最后交给 JJtomato 的“图集整理器”拼入指定行。不能把 6 张任意尺寸照片直接导入。
- 只使用自己拥有权利的图、提示词和参考素材；不要上传朋友照片、受限制的商业角色或含个人信息的图像。
- 图中出现文字、背景、方格、白底、角色断肢、每行角色变种、边缘被裁切时，应在导入预览中拒绝或要求重新生成。

### 5.3 导入器必须做的检查与反馈

导入不成功时，错误必须指向用户可修复的问题，不能只说“上传失败”。

| 检查 | 失败提示示例 | 处理 |
| --- | --- | --- |
| 文件名 | `缺少 avatar-atlas.png` | 提示精确文件名与位置 |
| MIME/魔数 | `该文件不是有效 PNG/WebP 图像` | 拒绝，防止伪装文件 |
| 尺寸 | `图集为 1024×1024，应为 1024×960` | 拒绝，不自动拉伸 |
| 透明背景 | `检测到大面积不透明背景，请用透明 PNG 重导出` | 拒绝或让用户回到 AI 重生成 |
| 格子内容 | `第 2 行第 5 格为空/角色越界` | 标出行列、显示局部预览 |
| 连贯性 | `第 3 行角色颜色或体型差异过大` | 标为人工确认项，不做静默裁切 |
| 体积 | `压缩后资源超过 1.20 MB` | 提供“自动压缩为 WebP”选项，压缩后再次校验 |
| 恶意压缩包 | `资源包包含未允许文件或异常解压大小` | 拒绝，不解压到可执行目录 |

自动压缩仅能改变编码质量，不能改变尺寸、裁切内容或补帧；任何会影响画面的修复都要在预览里由用户确认。通过后保存到应用数据目录（示例）：

```text
<appData>/JJtomato/avatars/<avatar-id>/<revision>/
├─ manifest.json
├─ preview.webp
└─ atlas.webp
```

## 6. 本地加载、导出与手动分享

### 6.1 加载与切换

- 用户在“我的形象”列表选择一个本地包后，更新个人资料的 `avatarRef`；切换不会改变房间、好友、任务或历史。
- 加载前先读取并校验 manifest，之后以临时对象 URL 预载 `preview.webp` 和 `atlas.webp`；两者都成功才原子替换当前形象，失败保持旧形象。
- 软件永远内置 `default-cat@1`，自定义包坏掉、缺资源、离线缓存失效或版本过旧时立即回退它，计时和聊天不能因此中断。

### 6.2 导出

用户点击 **导出我的形象** 后：

1. 软件从已验证的本地目录打成 `my-avatar.jjtpet`；不得打进聊天记录、房间 ID、好友列表、任务、用户 ID、登录 token 或上传 URL。
2. 下载/保存对话框默认文件名为 `<avatar-id>-r<revision>.jjtpet`。
3. 导出前显示：`这是可被他人导入的形象副本，不会自动把它设为对方的头像。`
4. 对方将 `.jjtpet` 拖入“我的形象 → 导入资源包”后，先完整校验；如果想编辑，必须另存为新的本地 `avatarId`，不能伪造原作者的线上 revision。

手动导出只是一种离线私人分享方式，不会自动出现在好友的列表里，也不需要网络账号。

## 7. 上传与不同用户之间的同步

### 7.1 分享权限

用户点击 **分享给好友** 时，先出现一个明确的选择：

```text
将「糯米小猫」共享给：
[ ] 所有已加入的自习室成员
[ ] 仅当前自习室：晚自习

好友只能下载并显示此形象，不能修改你的线上版本。
[取消] [确认上传]
```

默认不共享；取消共享后，未来请求不能再下载该资源，但已经缓存的本地副本可以显示到缓存过期或用户手动清除。服务端不能撤回已经被用户手动导出的 `.jjtpet`，界面必须讲清这个限制。

### 7.2 上传协议（后端实现目标，不是当前本地版接口）

上传必须走对象存储的短时签名 URL，应用服务器只处理鉴权、元数据、权限与事件：

```text
桌面端 -> POST /v1/avatar-uploads/init
         { manifest, atlasSha256, previewSha256, byteSizes }
服务端 -> 校验登录、频率、schema；返回 uploadId + 两个短时 PUT URL
桌面端 -> PUT preview.webp / atlas.webp 到对象存储
桌面端 -> POST /v1/avatar-uploads/{uploadId}/complete
服务端 -> 重新校验哈希、病毒/格式扫描、写入不可变 avatarId@revision
服务端 -> ProfileAvatarUpdated 事件（仅元数据）
```

约束：

- 文件上传上限严格使用第 4 节，服务端再次解码检查，不信任客户端结果。
- 同一个文件哈希可做去重存储，但权限记录必须逐用户保留，不能因哈希相同而泄露给别人。
- 每个 `avatarId@revision` 是不可变对象；用户更新形象时创建 `revision + 1`，完成预载后才更新 Profile 指针。
- 失败的上传不改变当前线上形象；断网后保留“待上传”草稿，可重试或删除。
- 上传和下载都必须 HTTPS；签名 URL 最短有效期，不能写进日志或聊天消息。

### 7.3 同步到好友的精确流程

```text
甲保存并选择形象 A@3
        ↓（甲显式授权给包含乙的房间）
服务端写入甲的 Profile.avatarRef = A@3
        ↓
向房间成员发送 ProfileAvatarUpdated { memberId, avatarId, revision, hashes }
        ↓
乙的客户端更新成员资料，但不立即下载大图
        ↓（A 在当前自习室或乙勾选它显示于小窗时）
乙请求受权限保护的 preview，再按需请求 atlas
        ↓
校验 SHA-256 成功 → 原子写入本地缓存 → AvatarRenderer 换图
失败/离线 → 使用 default-cat 或旧版缓存，并显示“形象同步中”
```

注意事项：

- Presence WebSocket 事件只带 `memberId`、`activity`、`stateVersion` 等几十字节字段，**不带图片**。
- 资源下载按可见性懒加载：好友列表先取 `preview.webp`；只有显示在主自习室/桌边小窗且允许动画时才取 `atlas.webp`。
- 资源缓存以 `avatarId/revision/sha256` 为键，下载完成后再一次性替换；不能让一半新帧、一半旧帧混着播放。
- 同一好友连续改形象时，客户端只保留最新指针和正在显示的旧版；过时下载完成后比较 revision，不应用旧文件。
- 服务器要保留至少一个历史 revision，直至没有在线 Profile 指向它且超过保留期；删除资源前必须先把用户指针切换到默认形象。
- 好友从房间移除、撤销分享、拉黑或退出账号后，客户端停止后续下载并清除该好友受限缓存；已在屏幕上的画面下一个渲染批次回退默认。

### 7.4 谁能修改、复制和使用

- 线上形象的所有者由登录账号决定，不能相信 `manifest.json` 中自称的作者字段。
- 好友只能显示已获得授权的 `AvatarRef`，不能向该 `avatarId` 上传新 revision。
- 好友手动导入 `.jjtpet` 后可“复制为我的形象”，此操作生成新的本地 ID（如 `cat-mochi-copy-20260827`）；如果上传则归属复制者的账号，原包仅保留 `source` 作为可选致谢文本。
- 房间管理员不能替成员强制更换形象、强制上传或查看未分享的本地包。

## 8. 轻量化预算与缓存策略

| 场景 | 限制 | 原因 |
| --- | --- | --- |
| 一个 atlas 解码后 | 约 3.75 MiB RGBA | 1024×960×4，必须控制同时解码数 |
| 主自习室全动画 | 最多 4 只；其余 `preview.webp` 静态 | 兼顾多人感与老电脑内存 |
| 置顶小窗全动画 | 最多 2 只；其余每秒 1 帧或静态 | 小窗不应成为 CPU 热点 |
| 前台动画 | 使用离散帧/`steps()`，按动作帧率播放 | 不用 60 FPS `requestAnimationFrame` |
| 后台/隐藏/省动效 | 冻结一帧；关闭定时器 | 计时由主进程 deadline 保证准确 |
| 磁盘缓存 | 最多 50 MB、LRU 清理、优先删非好友/旧 revision | 防止好友多时长期膨胀 |
| 网络 | preview 懒加载，atlas 可见时再加载；失败指数退避 | 避免打开房间就下载全员资源 |

当系统内存压力高、GPU 初始化失败或用户开启“极简模式”时，统一降级为：只显示 `preview.webp` 静态头像 + 状态色点，绝不影响待办、历史、倒计时、共同专注或消息。

## 9. 需要避免的逻辑错误

| 风险 | 约束/处理 |
| --- | --- |
| 两个场景状态不一致 | 两处只读同一 `RoomSnapshot`，禁止各自保存 `isTyping` 或好友状态 |
| 好友暂停导致我也暂停 | `focusSessionId` 关联，不共享控制权；只有本人操作自己的计时器 |
| 浏览另一房间后小窗突然换人 | 小窗只跟随 `activeRoomId`，浏览态不影响它 |
| 一个朋友显示两次 | 渲染键固定为 `roomId + memberId`；座位排序去重 |
| 资源更新闪烁/破图 | 新资源完整下载、哈希校验、预载成功后原子切换 |
| 角色挡住倒计时/下方 UI | 场景使用固定锚点和最大人数，溢出显示 `+N`，不自由堆叠 |
| 用户图里带桌子导致场景冲突 | 导入检查与 Pose Contract 明确拒绝场景/道具/文字 |
| 自制压缩包执行代码 | 只解包三种白名单文件、校验路径和 MIME，不执行任何包内文件 |
| 频繁输入事件耗电/泄露隐私 | 本地去抖后仅发送状态变化；原始键值永不跨 IPC/网络/日志 |
| 房间事件乱序 | 每个成员用 `stateVersion` 丢弃旧事件；服务器时间只用于展示与过期 |
| 旧版客户端不认识新形象 | 根据 `specVersion` 回退 `default-cat@1`，不阻塞入房 |

## 10. 分阶段开发清单与验收

### Wave 0：先冻结模型与回归测试

- [ ] 在 `src/core` 新建 `room`、`presence`、`avatar` 纯函数模型；将 `activity` 改为枚举，移除用文案表达状态的路径。
- [ ] 将本地演示的 `togetherCompanionId` 迁移为 `activeRoomId`、`focusSessionId`、成员列表；只做本地模拟，不假装网络已完成。
- [ ] 为暂停、继续、重置、跳过、自然结束、切换房间、断线过期和事件乱序写单元测试。
- 验收：已有“共同专注取消仍显示专注”的回归不能复发；一人多房间只会有一个当前房间。

### Wave 1：离线形象资源与导入器

- [ ] 内置 `default-cat@1` 资源及 `JJPET-Lite` schema。
- [ ] 实现创建套件导出、PNG 两文件导入、`.jjtpet` 导入、WebP 转码、manifest 生成、大小/透明度/网格/路径安全校验。
- [ ] 完成我的形象预览、切换、导出和失败提示。
- 验收：有效包可导入、导出再导入；恶意 ZIP、错误尺寸、白底图、额外脚本文件均被拒绝；失败不覆盖旧形象。

### Wave 2：统一渲染器与两个场景

- [ ] 建立 `AvatarRenderer`，输入只为 `AvatarRef + activity + renderMode + reducedMotion`。
- [ ] 实现主页面固定自习室（最多 8 位）和桌边小窗（最多 4 位、用户勾选）。
- [ ] mini BrowserWindow 接入精简 `RoomSnapshot` IPC；主进程仍保持计时权威。
- 验收：同一好友在两处显示同一形象和同一动作；暂停/继续/结束的视觉切换不超过一批状态更新；小窗人数溢出不遮挡计时。

### Wave 3：真实房间、权限与实时同步

- [ ] 实现账号、好友关系、RoomMember、Presence、消息和互动的最小服务端；只同步用户授权字段。
- [ ] 实现状态心跳、断线重连、`stateVersion` 去重和共同专注事件。
- [ ] 先做资源元数据与受保护下载，再做上传与 revision 更新。
- 验收：两台机器加入同一房间可在 1 秒目标内看到状态变化；断网后本地计时继续；重连不会重复发互动或覆盖新状态。

### Wave 4：形象上传与好友同步

- [ ] 实现第 7.2 节的 Init → PUT → Complete 流程、哈希校验、权限收敛、版本不可变和本地缓存 LRU。
- [ ] 实现“仅当前房间/所有允许房间”的分享选择、撤销分享、复制为我的形象。
- [ ] 为过时 revision、下载失败、权限撤销、缓存清理、同哈希去重写集成测试。
- 验收：甲更新形象后乙在可见时懒加载；乙不能编辑甲的线上版本；撤销后无新下载；任一失败均回退默认而不中断专注。

### Wave 5：性能与可访问性实测

- [ ] 在目标老电脑实测冷启动、空闲 RSS、1/4/8 人场景 CPU、8 小时内存曲线和隐藏窗口功耗。
- [ ] 测试“极简模式”、系统省电、窗口最小化、`prefers-reduced-motion`、高 DPI、缩放 125%/150%。
- [ ] 依据实测调整帧率和缓存上限，不能只凭理论声称轻量。
- 验收目标：主窗口空闲内存、CPU 和计时准确性继续符合 `JJtomato-latest-product-solution.md` 第 15 节；宠物降级不破坏核心待办体验。

## 11. 交付前检查表

- [ ] 默认形象无需联网即可使用；自定义形象默认不上传。
- [ ] 用户能在不编辑 JSON 的情况下创建、导入、预览、保存、导出形象。
- [ ] 包内确实只有 `manifest.json`、`preview.webp`、`atlas.webp`，文件名和尺寸都符合第 4 节。
- [ ] 6 行动作同一角色、透明、无背景/道具/文字，且基线稳定。
- [ ] 主页面和置顶小窗读取同一 RoomSnapshot；两个场景从不各自写状态。
- [ ] 多房间下 `activeRoomId` 唯一，浏览行为不会切换小窗或中断专注。
- [ ] 图像资源和 Presence 分通道同步；图像不经 WebSocket；资源校验 SHA-256 后才切换。
- [ ] 好友权限、撤销分享、断网回退、旧版本兼容和缓存上限都有测试。
- [ ] 用户能关闭每一位朋友在置顶小窗的显示，也能一键关闭所有动画。

## 12. 当前实现的状态

目前仓库已经有本地好友列表、消息、互动和“一起坐下”的演示逻辑，但没有真实账户、Room 服务、WebSocket、资源上传、`.jjtpet` 解析器或上述双场景渲染器。因此本文把这些能力分为 Wave 0–5；开发应先完成 Wave 0 和 Wave 1 的纯本地闭环，再接入联网同步，避免先做界面后补状态模型而产生“取消后仍显示一起专注”一类错误。
